<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="com.dexcom.cgm.EXTERNAL_PERMISSION" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- DND override for URGENT medical alarms: lets a bypass-DND notification channel ring through
         Do Not Disturb / bedtime modes. Special access — the user must still grant it in system settings. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <uses-permission-sdk-23 android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Remove legacy storage permissions pulled in by library manifest merge.
         App uses scoped storage / SAF (minSdk 31). -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <!-- To receive data from xdrip. -->
    <uses-permission android:name="com.eveningoutpost.dexdrip.permissions.RECEIVE_BG_ESTIMATE" />

    <!-- To receive data from Aidex -->
    <uses-permission android:name="com.microtechmd.cgms.aidex.permissions.RECEIVE_BG_ESTIMATE" />

    <!-- 🏥 Health Connect - MTR Steps Integration (Android 14+) -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_BASAL_BODY_TEMPERATURE" />

    <!-- Health Connect availability query -->
    <!-- Health Connect availability query (Android 14+ requirement) -->
    <queries>
        <!-- Detect if Health Connect is installed -->
        <package android:name="com.google.android.apps.healthdata" />

        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <application
        android:requestLegacyExternalStorage="true"
        android:name="app.aaps.MainApp"
        android:allowBackup="true"
        android:backupAgent="app.aaps.utils.SPBackupAgent"
        android:fullBackupOnly="false"
        android:icon="${appIcon}"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:restoreAnyVersion="true"
        android:roundIcon="${appIconRound}"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">
        <profileable android:shell="true" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.backup.api_key"
            android:value="AEdPqrEAAAAI3JiApyMrbP2QFzZ2fYfCPsgjkRp53Dm2S1-zPQ" />

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name="app.aaps.ComposeMainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- AIMI / tooling: classic activities (Dagger Android), parent = Compose shell -->
        <activity
            android:name="app.aaps.activities.DashboardPreviewActivity"
            android:exported="false"
            android:parentActivityName="app.aaps.ComposeMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="app.aaps.activities.ComparatorActivity"
            android:exported="false"
            android:label="@string/comparator_title"
            android:parentActivityName="app.aaps.ComposeMainActivity"
            android:theme="@style/AppTheme" />
        <!-- Receive new BG readings from other local apps -->
        <receiver
            android:name="app.aaps.receivers.DataReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <!-- Receiver from xDrip -->
                <action android:name="com.eveningoutpost.dexdrip.BgEstimate" />
                <!-- Receiver from 640g uploader -->
                <action android:name="com.eveningoutpost.dexdrip.NS_EMULATOR" />
                <!-- Receiver from glimp -->
                <action android:name="it.ct.glicemia.ACTION_GLUCOSE_MEASURED" />
                <!-- Receiver from Dexcom -->
                <action android:name="com.dexcom.cgm.EXTERNAL_BROADCAST" />
                <action android:name="com.dexcom.g7.EXTERNAL_BROADCAST" />
                <!-- Receiver from Poctech -->
                <action android:name="com.china.poctech.data" />
                <!-- Receiver from Tomato -->
                <action android:name="com.fanqies.tomatofn.BgEstimate" />
                <!-- Receiver from GlucoRx Aidex -->
                <action android:name="com.microtechmd.cgms.aidex.action.BgEstimate" />
                <!-- Receiver from patched Ottai app (International) -->
                <action android:name="info.nightscout.androidaps.action.OTTAI_APP" />
                <!-- Receiver from patched Syai Tag app -->
                <action android:name="info.nightscout.androidaps.action.SYAI_TAG_APP" />
                <!-- Receiver from patched Ottai app (China) -->
                <action android:name="cn.diyaps.sharing.OT_APP" />
                <!-- Receiver from patched SI app -->
                <action android:name="cn.diyaps.sharing.SI_APP" />
                <!-- Receiver from patched Sino app -->
                <action android:name="cn.diyaps.sharing.SINO_APP" />
                <!-- Receiver from Instara App -->
                <!-- NOTE: The action name for Instara App is still called Teljane -->
                <action android:name="info.nightscout.androidaps.action.Teljane_APP" />
            </intent-filter>
        </receiver>

        <!-- Receive new SMS messages -->
        <receiver
            android:name="app.aaps.receivers.SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Receive ignore 5m, 15m, 30m requests for carb notifications -->
        <receiver android:name="app.aaps.plugins.aps.loop.CarbSuggestionReceiver" />

        <!-- Auto start -->
        <receiver
            android:name="app.aaps.receivers.AutoStartReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <!-- AIMI Health Connect Permission Request (Promoted to Main Manifest for Discovery) -->
        <activity
            android:name="app.aaps.plugins.aps.openAPSAIMI.physio.AIMIHealthConnectPermissionActivityMTR"
            android:exported="true"
            android:label="Health Connect Permissions"
            android:theme="@style/AppTheme">
            <!-- CRITICAL: This Intent Filter is how Health Connect "Discovers" the app -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
                <category android:name="androidx.health.category.HEALTH_PERMISSIONS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Android 14+ "Manage Access" Link -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity="app.aaps.plugins.aps.openAPSAIMI.physio.AIMIHealthConnectPermissionActivityMTR"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- WorkManager on-demand initialization: remove the default initializer so MainApp's
             Configuration.Provider (with HiltWorkerFactory) is used instead. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- WorkManager (Android 14+): SystemForegroundService must declare a type when targetSdk >= 34.
             Without this merge, startForeground() can run as FGS type "none" and throw InvalidForegroundServiceTypeException. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:directBootAware="false"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>

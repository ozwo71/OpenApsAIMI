# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx10248m -XX:MaxPermSize=256m
org.jetbrains.dokka.experimental.gradle.pluginMode=V2EnabledWithHelpers
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true
org.gradle.warning.mode=all
org.gradle.jvmargs=-Xmx8g -XX:+UseParallelGC -Xss1024m
org.gradle.workers.max=12
kotlin.daemon.jvm.options=-Xmx2g

android.enableJetifier=false
android.useAndroidX=true
android.nonTransitiveRClass=true
# Cache is causeing issues with CircleCI nad maybe Studio 2021
# org.gradle.unsafe.configuration-cache=true

android.nonFinalResIds=true

# AGP 9+: tensorflow-lite and tensorflow-lite-gpu both declare namespace org.tensorflow.lite in their
# AAR manifests. Allow the merger to proceed until TensorFlow ships distinct namespaces (see AGP 9 release notes).
android.uniquePackageNames=false

# Prevent KSP errors on this fork (ONNX/TFLite + large KSP graph). Upstream uses true; keep false until stable.
ksp.incremental=false
# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true

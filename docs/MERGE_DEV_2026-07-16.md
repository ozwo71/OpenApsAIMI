# Merge log — `dev` → `dev_OAPSAIMI_mergeDEV` (2026-07-16)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target
- **Target:** `dev_OAPSAIMI_mergeDEV` (at `4e3e8650bd` before merge; includes T3C Autodrive basal authority).
- **Source:** `milos/dev` @ `638f23dfab` (Automation: lints).
- **37 incoming commits** since merge-base `d389d5e1c2` (last documented merge 2026-07-10).
- Working tree clean before merge.
- Merge commit: `20b7aa51e6`.

## Scope of incoming commits (high level)

| Area | Summary |
|------|---------|
| Navigation | `ElementType` / `ElementCategory` moved to `core.interfaces.navigation` |
| Permissions | Full-screen intent permission path (#2) for alarms |
| Insulin | Concentration UI fixes; `PumpWithConcentrationImpl` zero-bolus short-circuit (already aligned with fork) |
| Bolus | BolusWizard source management; cancel progress alert fix; very small bolus deliverTreatment |
| Automation | Icon color refactor (`AutomationColors`); profile silent gate; lint cleanup |
| Wear | Per-flavor tile header |
| NSCv3 | Prevent memory leak |
| Omnipod Eros | Infinite connecting loop after pod deactivation |
| Libs | junit 6.1.2, glide-compose, firebase-bom, tink-android |

**No changes to AIMI / SMB / autoISF determine_basal logic** in upstream diff → **nothing to port** into
`DetermineBasalAIMI2` or OpenAPS engines.

## Conflicts (2) — combine, not theirs-only

| File | Resolution |
|------|------------|
| `gradle.properties` | **Combine:** keep fork `android.uniquePackageNames=false` (TFLite dual namespace) + `ksp.incremental=false`; take upstream `org.gradle.tooling.parallel=true`. |
| `ui/.../MainDrawer.kt` | **Combine:** keep fork `AppBrandIcon`; take upstream `import app.aaps.core.interfaces.navigation.ElementType`. |

## Transitive fork fixes (ElementType package move)

Upstream deleted `core.ui...ElementType`. Two fork-only files still imported the old package:

| File | Fix |
|------|-----|
| `DashboardQuickActionsBar.kt` | Import → `core.interfaces.navigation.ElementType` |
| `AfrezzaDialogScreen.kt` | Import → `core.interfaces.navigation.ElementType` |

`AFREZZA` enum entry preserved in auto-merged `ElementType` / `ElementTypeStyle`.

## Fork preservation verified (post-merge tree)

- **AIMI:** `OpenAPSAIMIPlugin` @225 in `ApsPluginsListModule`; hormonitor export + T3C Autodrive basal authority intact.
- **Adaptive smoothie:** `AdaptiveSmoothingPlugin` @615; `calibratedOrValue` unchanged.
- **Dashboard skin:** `OverviewPlugin` @20; `dashboardOverview` in `MainScreen.kt` preserved (auto-merge).
- **Eversense:** `EversensePlugin` @445; `:plugins:eversense` in `settings.gradle`; `SourceSensor` E3/365 unchanged. Constraint satisfied without re-application.
- **ML / physio:** `AndroidManifest` — `MANAGE_EXTERNAL_STORAGE`, Health Connect activities, `USE_FULL_SCREEN_INTENT` present.
- **DB vacuum:** `KeepAliveWorker.databaseCleanup` → `runVacuum = false` unchanged.
- **Hormonitor:** no structural changes in upstream diff; exporter calls preserved.
- **`:pump:apex`:** still in `pumpExclusions` in `app/build.gradle.kts`.

## AIMI docs review

All `docs/AIMI*.md` remain accurate (no PluginsListModule / ElementType package claims to fix).
Checklist + Eversense constraint docs updated to point at this log.

## Build / test

- `./gradlew :app:compileFullDebugKotlin :wear:compileFullDebugKotlin` — **PASS** (after
  `AutomationIconRaster` ElementType color migration).
- `:plugins:smoothing:testFullDebugUnitTest` — `AdaptiveSmoothingPluginTest` **PASS**.
- `:plugins:aps:testFullDebugUnitTest` — `T3cAutodriveBasalBridgeTest` **PASS**.

## Pending (user, on-device — NON_REGRESSION §4)

- [ ] Cold start, no ANR first 5 minutes.
- [ ] Dashboard ↔ original overview skin toggle.
- [ ] AIMI + adaptive smoothie enabled paths.
- [ ] JSON/CSV ML writes; Health Connect physio permissions.
- [ ] Alarms / full-screen intent path (new upstream work).
- [ ] Eversense source still selectable if used.

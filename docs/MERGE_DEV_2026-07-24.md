# Merge log — `dev` → `dev_OAPSAIMI_mergeDEV` (2026-07-24)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target

- **Target before merge:** `dev_OAPSAIMI_mergeDEV` @ `97c72720fa`.
- **Source:** Nightscout `dev` / `milos/dev` @ `ab88d5f1db` (refreshed before merge).
- **Merge base:** `638f23dfab`.
- **Incoming:** 36 upstream commits, 143 upstream paths.
- **Merge commit:** `08b53ac923`.
- **Merge result:** 146 files changed, 5,795 insertions, 352 deletions.

## Incoming scope

- APS crash prevention: non-finite diagnostics and invalid ISF/CR/autosens guards.
- Insulin configuration echo handling and nullable active concentration UI.
- Profile-switch synchronization fixes.
- Extended-bolus duplicate-timestamp guard.
- Equil, ComboCtl, Dana emulator and Omnipod expiry fixes.
- Wear BG graph complication and tap actions.
- Emulator E2E seams, CI sharding and dependency updates.
- Crowdin resource updates.

## Conflicts (3)

Every conflict was reviewed individually; no bulk ours/theirs resolution was used.

1. `.github/workflows/aaps-ci.yml` — **combine**
   - Preserved fork `fork` / `upstream_tag` source selector and free-form upstream tag.
   - Updated the example/default upstream tag to `3.4.2.4`.
   - Kept upstream `actions: write` permission for cleanup.
2. `buildSrc/src/main/kotlin/Versions.kt` — **keep fork version, acknowledge upstream line**
   - Preserved `4.0.0.0-dev.AIMI.200726` and `targetSdk = 34` for Health Connect compatibility.
   - Updated the comment to record upstream `dev-b`.
3. `plugins/constraints/src/main/res/values/strings.xml` — **combine**
   - Preserved fork `disable_Libre_smb_restrictions`.
   - Restored upstream `bg_doubled_lgs`.

## AIMI parity for upstream APS safety

Upstream commit `4eeb82e002` changed SMB and AutoISF but did not touch fork-only AIMI.
The safety intent was ported without changing public signatures:

- `OpenAPSAIMIPlugin` now aborts before `determine_basal` when sensitivity, carb ratio,
  autosensitivity ratio, or active dynamic sensitivity is non-finite or non-positive.
- `DetermineBasalAIMI2.round(Double)` retains its NaN → 0 fallback and now emits the same
  `roundNaN=NaN` diagnostic token consumed by non-finite APS-result reporting.
- The three asynchronous AIMI ML paths remain unchanged:
  - SMB refinement (`AimiSmbTrainer`);
  - basal/T3C training (`BasalMlTrainingCoordinator`, shared mutex);
  - Autodrive attention training (`AutodriveNeuralTrainer`).

## Fork preservation verified

- **AIMI registration:** `OpenAPSAIMIPlugin` remains bound at `@IntKey(225)`.
- **Adaptive Smoothie:** registration `@IntKey(615)` and `calibratedOrValue` inputs preserved.
- **Dashboard:** embedded dashboard plus original Overview skin path (`dashboardOverview`) preserved;
  `OverviewPlugin` remains `@IntKey(20)`.
- **Eversense:** `:plugins:eversense`, `EversensePlugin` @445, E3/365 `SourceSensor` values,
  DB mappings and notification-reader v3 preserved.
- **ML storage:** `MANAGE_EXTERNAL_STORAGE`, app-scoped/internal fallbacks and JSON/CSV paths preserved.
- **Physio:** Health Connect permissions and foreground health/data-sync declarations preserved.
- **Hormonitor:** current schema `1.4.0`, additive `patient_story`, and tolerant historical reader preserved.
- **DataInbox:** drain-before-return remains present for Dexcom, xDrip, SMS and APS result export;
  cancellation requeue remains present for high-frequency CGM workers.
- **Database:** automatic cleanup remains `runVacuum=false`; full VACUUM stays manual.
- **Pump wiring:** fork `:pump:apex` exclusion remains in `app/build.gradle.kts`.

## Validation performed

Passed:

- `:app:compileFullDebugKotlin`
- `:wear:compileFullDebugKotlin`
- `:plugins:smoothing:testFullDebugUnitTest`
- `:plugins:eversense:testFullDebugUnitTest`
- isolated Dexcom/xDrip DataInbox worker tests after resetting xDrip static throttle state per test
- isolated APS result export + insulin implementation tests after aligning the export test with
  mandatory drain-before-return behavior
- upstream Wear `BgGraphComplicationTest`
- Equil unit-test task reported no failure in the combined Wear/Equil invocation
- IDE diagnostics on reviewed merge/AIMI files

Known baseline test-suite issues not caused by incoming `dev` paths:

- Three AIMI scenario tests still expect a null final feature vector, while production now supplies
  the causal feature vector. The affected AIMI code predates this merge.
- The full Wear suite has 12 failures in `ActionSourceTest` / `StaticTileSourceTest`; the new BG graph
  complication tests pass.
- `:ui:compileFullDebugUnitTestKotlin` is blocked by stale constructor fixtures in
  `GraphViewModelTest` and `RunningModeManagementViewModelTest`; main app/UI production compilation passes.
- Several `KeepAliveWorkerTest` fixtures fail independently of this merge; the production
  `runVacuum=false` invariant was inspected directly and upstream did not touch that worker.

Not run:

- Android instrumented / managed-device E2E tests (require explicit permission).
- On-device smoke tests.

## AIMI Markdown audit

All 39 `docs/AIMI*.md` / `docs/aimi*.md` files were included in the merge audit search.
Current-state schema references were aligned to Hormonitor `1.4.0` in:

- `AIMI_HORMONITOR_STUDY_NOTES_2026-06-06.md`
- `AIMI_PHYSIO_RUNTIME_ACTIVATION_2026-06-06.md`
- `AIMI_PATIENT_MODE_REPLAY_CHECKLIST_2026-06-06.md`

Historical `1.1.0`–`1.3.0` descriptions remain intentionally documented.

## Pending user/device validation

- Cold start and five-minute idle without ANR.
- Dashboard embedded skin ↔ original Overview skin switching.
- AIMI and Adaptive Smoothie enabled paths, including Linear calibration.
- JSON/CSV writes with and without all-files permission.
- Health Connect physio permissions and live refresh.
- Hormonitor `1.4.0` export plus historical `1.1.0` reader compatibility.
- Eversense selection and E3/365 source persistence if used.
- Insulin management edit/save without false external-update loops.

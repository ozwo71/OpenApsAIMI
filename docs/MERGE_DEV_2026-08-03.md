# Merge log — `dev` (milos) → `feature/dexcom-oneplus-native` (2026-08-03)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md),
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md), and
[MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md](MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md).

## Source / target

- **Target:** `feature/dexcom-oneplus-native` (pre-merge `d4914694a2`, safety tag `premerge-dev-20260803`).
- **Source:** local `dev` @ `fa2d2c78a5` (tracks `milos/dev`, fetched 2026-08-03).
- **Merge-base:** `88d31b816d` (previous documented merge 2026-07-31).
- **Incoming:** 45 commits. Working tree was clean before merge.

## Scope of incoming commits (high level)

| Area | Summary |
|------|---------|
| Alarms | Refactor: `USE_FULL_SCREEN_INTENT` dropped as a requested permission; background alarms now wake the screen via `AlarmManager.setAlarmClock()` (`AlarmScreenWakeReceiver`) + notification "Mute" action (`AlarmMuteReceiver`) |
| APS diagnostics | `DetermineBasalResult.reportNonFiniteResultFields()` — logs + Crashlytics-reports which APS field is NaN/±Inf before the kotlinx serialize crash; `FabricPrivacy` added to the constructor |
| AAPSClient | Autosens ratio shown from `openAPSData.suggested.sensitivityRatio` (`ChipsViewModel`) |
| Equil | Direct connect without scan (#5040), prime fix, `SCAN_MODE_LOW_LATENCY` |
| Wear | WFF auto-install / Samsung watchface activation, LongStatus + Sgv complication unification, back-navigation, bolus progress, carbs bound to COB, predictions pushed to AAPSClient watch |
| Tests / CI | `RunningModeReconciler` `@VisibleForTesting` accessors + `@Volatile`, flaky-test fixes, reporting disabled in tests, CircleCI/aaps-ci updates, version 3.4.2.6 |
| Deps | commons-codec 1.22.1, firebase-bom 34.17.0, Crowdin translations |

**No upstream change to `determine_basal`, SMB, AutoISF, AIMI, smoothing, dashboard, `SourceSensor`, DI/plugin registration, DB or storage paths** (see "Fork preservation" below).

## Conflicts — decisions (4, none fork-critical, no theirs-only on fork paths)

| File | Resolution |
|------|------------|
| `app/src/main/AndroidManifest.xml` | **Combine** — kept fork AIMI/tooling activities (`DashboardPreviewActivity`, `ComparatorActivity`) **and** added upstream `AlarmScreenWakeReceiver` / `AlarmMuteReceiver`. Both sides were purely additive. |
| `implementation/.../plugin/PluginStore.kt` | **Combine** — accepted upstream removal of the `USE_FULL_SCREEN_INTENT` permission group + its `isMissing` branch, kept the fork `ACCESS_NOTIFICATION_POLICY` (DND-override) group and its branch. |
| `pump/equil/.../ble/EquilBLE.kt` | **Combine** (2 hunks) — `disconnect()` uses the fork helper `cancelPendingConnect()` + upstream `stopScan()` (superset: `stopScan()` clears `startTrue`); `connect()` takes upstream direct-connect-by-MAC **and** keeps the fork 15 s connect watchdog (`TIME_OUT_CONNECT_WHAT` / `EQUIL_BLE_CONNECT_TIMEOUT_MS`), which upstream has no equivalent for and which matters more with `autoConnect`. |
| `.github/workflows/aaps-ci.yml` | **Ours** — fork `source: fork \| upstream_tag` + `tagName` inputs kept (upstream replaced the input with a version list). Carried over the new upstream tag as the default: `3.4.2.4` → `3.4.2.6`. |

### Transitive fix (not a conflict, would have broken the build)

- `PluginStore.kt`: upstream deleted `import android.app.NotificationManager` along with its FSI branch, but the fork's `ACCESS_NOTIFICATION_POLICY` branch still uses `NotificationManager`. **Import restored.**

## AIMI / APS parity review (upstream → AIMI)

| Upstream change | AIMI impact | Action |
|-----------------|-------------|--------|
| `DetermineBasalResult.reportNonFiniteResultFields()` | **Inherited automatically** — AIMI builds its result through the same `apsResultProvider.get().with(it)`, so a non-finite AIMI field is now named in the log/Crashlytics with `algorithm=AIMI` | None needed |
| `ChipsViewModel` autosens ratio | Algorithm-agnostic (reads the active APS result / NS devicestatus) | None needed |
| `RunningModeReconciler` `@VisibleForTesting` + `@Volatile` | Test-only accessors, no dosing path | None needed |
| Alarms refactor | Shared `implementation` module; fork bypass-DND channels (`_alarm_bypass`), `AlarmSoundPlayer` OWNER_FULLSCREEN path and `AIMI_AUDITOR_INSIGHT` id all preserved | None needed |

**Nothing had to be ported into AIMI for this merge.**

### Pre-existing drift found during the parity review (NOT introduced by this merge — open item)

`OpenAPSSMBPlugin` has a **wider** `invalidInputs` guard than `OpenAPSAIMIPlugin` / `OpenAPSAutoISFPlugin`:

| Check | SMB | AutoISF | AIMI |
|-------|-----|---------|------|
| `sens`, `carb_ratio`, `autosens.ratio` finite & > 0 | ✅ | ✅ | ✅ |
| `variable_sens` finite & > 0 under dynISF | ✅ | ✅ | ✅ |
| `TDD` finite & > 0 under dynISF | ✅ | ❌ | ❌ |
| `insulinDivisor > 0` under dynISF | ✅ | ❌ | ❌ |
| dynISF gate is `dynIsfMode && dynIsfResult.tddPartsCalculated()` | ✅ | ❌ (raw mode) | ❌ (raw mode) |
| `fabricPrivacy.logException` + `EventOpenAPSUpdateGui` on abort | ✅ | ❌ | ❌ |

AIMI is currently **aligned with upstream AutoISF**, not with SMB. Structurally, AIMI's `insulinDivisor` is a
constant 45/55/65 (never ≤ 0), so only the `TDD` check and the abort observability are actually missing.
Adding the `TDD` check **must not** be done under the raw `dynIsfMode` flag — AIMI has no
`dynIsfResult.tddPartsCalculated()` equivalent, so an unconditional TDD guard could abort the loop whenever
TDD is unavailable. **Left unchanged on purpose; needs an explicit decision before touching a dosing-abort
condition.**

## Fork preservation verified (post-merge tree, mechanical diff vs pre-merge baseline)

Full invariant script re-run before/after the merge; **output identical** except manifest line numbers
(shifted by the two added receivers).

| Gate | Evidence |
|------|----------|
| **AIMI** | 436 plugin files, `usingDynamicIsf` + `offersDynamicSensitivity` present, `@IntKey(225)` registered |
| **Adaptive smoothie** | 23 `calibratedOrValue` usages unchanged |
| **Dashboard + skin switch** | 5 `dashboardOverview` sites (`ComposeMainActivity` + `MainScreen`) and 9 `SkinDescriptionProvider` refs unchanged → user can still return to the original overview skin |
| **ML / physio permissions** | `MANAGE_EXTERNAL_STORAGE`, `READ`/`WRITE_EXTERNAL_STORAGE`, `ACTIVITY_RECOGNITION`, 7 Health Connect `health.READ_*`, `AIMIHealthConnectPermissionActivityMTR` (+ alias), FGS `dataSync` — all present |
| **Hormonitor** | 5 `patient_story` refs, 154 hormonitor refs across 12 files unchanged |
| **DB freeze gate** | `KeepAliveWorker` → `cleanupDatabase(..., runVacuum = false)`; `AppRepository.cleanupDatabase` still `runVacuum: Boolean = false` with no inline VACUUM |
| **DataInbox** | 6 `drain()` call sites unchanged |
| **Eversense** | `:plugins:eversense` included, `SourceSensor` `EVERSENSE` / `EVERSENSE_E3` / `EVERSENSE_365`, `@IntKey(445)` |
| **Dexcom ONE+** | `:plugins:dexcom_oneplus` + `libkeks` included, `SourceSensor.DEXCOM_ONEPLUS_NATIVE`, `@IntKey(446)`, notification-reader remaps `com.dexcom.d1plus` / `com.dexcom.dexcomone` → `AAPS-DexcomOnePlus` |
| **AIMI docs** | All 48 `.kt` references in `AIMI_ARCHITECTURE_MAP.md`, `AIMI_ROADMAP.md` and the merge-constraint docs still resolve to existing files |

Independently confirmed: the merge diff touches **no** file matching
`aimi|smooth|hormonitor|dashboard|Skin|MainApp|SourceSensor|eversense|dexcom|KeepAlive|AppRepository`.

## Validation run (this merge)

| Gate | Result |
|------|--------|
| `:app:compileFullDebugKotlin` | **PASS** |
| `:plugins:aps` unit tests (incl. AIMI) | **PASS** — 1231 tests, 0 failures |
| `:plugins:smoothing` unit tests | **PASS** — 14 tests, 0 failures |
| `:implementation` unit tests | 706 tests, **17 failed — all pre-existing** |
| `:core:interfaces` unit tests | **PASS** — 49 tests, 0 failures |
| `:ui` unit tests | **Does not compile — pre-existing** (see below) |
| On-device smoke (cold start, skin switch, AIMI JSON/CSV write, physio HC, ONE+, Eversense) | **Pending user confirmation** — not marked working |

### Pre-existing `:implementation` failures (proven, not a merge regression)

The pre-merge tree (`premerge-dev-20260803`) was checked out in a separate worktree and the same task run:
**703 tests / 17 failed**, with the **identical failure set** as post-merge (**706 tests / 17 failed** — the
3 extra tests are the new upstream `DetermineBasalResultTest`, all passing).

| Test class | Count | Cause (pre-existing) |
|------------|-------|----------------------|
| `KeepAliveWorkerTest` | 14 | Fork changed `KeepAliveWorker` from `loop.runningMode()` to `persistenceLayer.getRunningModeActiveAt(now).mode`, but the test (identical to upstream's) still stubs only `loop.runningMode()` → NPE on the unstubbed call |
| `HardLimitsImplTest` | 2 | Fork widened the DIA hard limits (min 4.0 / max 12.0) for AIMI/Afrezza; the upstream test still expects 5.0 / 10.0 |
| `LastBgDataImplTest` | 1 | `lastBg()` expected 70.0, got 200.0 — fork/upstream expectation drift |

**Follow-up (not blocking this merge):** these 17 are fork-vs-upstream test drift and should be fixed by
updating the fork's test stubs/expectations — not by reverting the production code.

### Pre-existing `:ui` test-source compile break (not a merge regression)

`:ui:testFullDebugUnitTest` does not compile: `GraphViewModelTest.kt:48` misses 7 constructor arguments
(`iobCobCalculator`, `decimalFormatter`, `loop`, `config`, `persistenceLayer`, `constraintChecker`,
`profileFunction`) that the fork added to the dashboard `GraphViewModel` without updating the test.

**Neither `GraphViewModel.kt` nor `GraphViewModelTest.kt` is touched by this merge** (the merge only touches
`MainViewModel`, `ChipsViewModel`, `TreatmentViewModel` and `TreatmentViewModelTest` under `ui/`), so the
mismatch predates it. `:ui` **main** sources compile fine — `:app:compileFullDebugKotlin` passes, which is
what ships. Follow-up: realign `GraphViewModelTest` with the fork's `GraphViewModel` constructor.

## Post-merge CI breakage (found and fixed after the merge commit)

Both `aaps-ci.yml` and `android-build.yml` failed on `:wear`. **Neither was caused by the workflow changes** —
both workflows run `:wear:assemble<Variant>`, and the merge had produced two silent *duplicate* artifacts that
git text-merges without a conflict and that no Kotlin compile or unit test can catch:

| Failing task | Cause | Fix |
|--------------|-------|-----|
| `:wear:mergeFullReleaseResources` | `wizard_result_total`, `wizard_insulin_format`, `wizard_result_correction_percentage` each declared **twice** in `wear/src/main/res/values-{bg-rBG,es-rES,ro-rRO}/strings.xml` — fork block + identical upstream Crowdin block merged side by side | Removed the second (merge-inserted) block in the 3 files; values were byte-identical |
| `:wear:processFullReleaseMainManifest` | `service .complications.BgGraphComplication` declared **twice** in `wear/src/main/AndroidManifest.xml` | Kept the upstream copy carrying the new `SAFE_WATCH_FACES` → `${applicationId}.watchfacepush.aapsv4` meta-data (WFF auto-install), removed the older duplicate |

**Why it was missed:** post-merge validation ran `:app:compileFullDebugKotlin` + unit tests only, which never
touch `:wear` resource or manifest merging. The checklist now requires a duplicate-entry scan and
`./gradlew :app:assembleFullDebug :wear:assembleFullDebug` (see §1).

**Verified after fix:** repo-wide duplicate scan clean; the two failing tasks pass; release build now stops
only at `:app:validateSigningFullRelease` (local keystore absent — CI supplies it via secrets);
`:app:assembleFullDebug :wear:assembleFullDebug` **BUILD SUCCESSFUL** (both APKs produced).

## Notes for follow-up

1. Decide on the AIMI `invalidInputs` TDD check + abort observability (see drift table above).
2. Fix the 17 pre-existing `:implementation` test failures (stub `getRunningModeActiveAt`, realign DIA and
   `lastBg` expectations).
3. Device smoke per checklist §4 — in particular dashboard ↔ original overview skin toggle, AIMI JSON/CSV
   write, physio Health Connect, ONE+/Eversense listing, and the **new** background-alarm path
   (screen wake without `USE_FULL_SCREEN_INTENT` + notification "Mute" action).
4. Still deferred from 2026-07-31: port upstream overview dynamic Y-scaling into the dashboard Vico graphs.

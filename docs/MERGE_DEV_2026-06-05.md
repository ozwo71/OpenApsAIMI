# Merge dev → dev_OAPSAIMI_mergeDEV (2026-06-05)

Upstream: Nightscout `dev` @ `496d3275f4` (`Improve google disk export`).

Merge-base with fork: `d1e22496f4` (previous merge [MERGE_DEV_2026-05-20.md](MERGE_DEV_2026-05-20.md)).

## Upstream commits integrated (8)

| Commit | Summary |
|--------|---------|
| `85b5bc1f8e` | Bump Vico compose 3.2.1 → 3.2.2 |
| `32d9187b39` | Dependabot Vico PR merge |
| `f434869e99` | URGENT → IMPORTANT (notification severity rename) |
| `937d77b82b` | Fix and unify alarm system (`AlarmSoundPlayer`, silent URGENT path) |
| `43d55d8f02` | Remove excessive logging |
| `ab79da7817` | Fix graphs without data (no early-return on empty BG; `lineModel`; `getXStep`) |
| `ee0c3b0f3e` | Tidepool: catch missing Chrome |
| `496d3275f4` | Improve Google Drive export |

## Conflict decisions

| File | Decision |
|------|----------|
| `NotificationId.kt` | **Combine** — upstream enum API (`defaultLevel`, `fromOrdinal`, no `legacyId`) + fork entries: Eversense (5), `HYPO_RISK_ALARM`, `APEX_SERVICE_MSG`, `PUMP_IS_LOCKED`, `PERMISSION_*` |
| `BgGraphCompose.kt` | **Combine** — fork dashboard markers/scenario projection + upstream empty-graph fix (`lineModel`, `getXStep`, no BG early-return) |
| `GraphUtils.kt` | **Combine** — upstream `@Composable`/`remember` imports + fork dashboard decoration helpers |
| `GraphViewModel.kt` | **Combine** — fork IOB/loop/profile imports + upstream `ProcessedDeviceStatusData` |
| `CobGraphCompose.kt`, `IobGraphCompose.kt` | **Theirs (deleted)** — merged upstream into `SecondaryGraphCompose.kt`; fork dashboard already uses `SecondaryGraphCompose` |

## Post-merge fork fixes (compile / alarm parity)

| File | Fix |
|------|-----|
| `OverviewFragment.kt` | Add `NotificationLevel.IMPORTANT` branch (alarm unification renames old URGENT tier) |
| `UiInteractionImpl.kt` | Replace removed `postSoundAlarmNotification` with `postSilentAlarmNotification` + `AlarmSoundPlayer` (legacy `NotificationStore` sound path) |

## Non-regression gates (code review)

| Gate | Status |
|------|--------|
| AIMI plugin / loop | **Preserved** — `DetermineBasalAIMI2` not in merge diff; `HYPO_RISK_ALARM` kept `URGENT` in `NotificationId` |
| Adaptive Smoothie | **Preserved** — `calibratedOrValue` in `AdaptiveSmoothingPlugin.kt` |
| Dashboard + skin switch | **Preserved** — `ComposeMainActivity.dashboardOverview`, `SkinDashboardPreferenceSync`, `OverviewUseDashboardLayout` |
| ML JSON/CSV writes | **Preserved** — manifest `WRITE_EXTERNAL_STORAGE` / `MANAGE_EXTERNAL_STORAGE`; AIMI export paths unchanged |
| Physio Health Connect | **Preserved** — `AIMIHealthConnectPermissionActivityMTR` in manifest |
| Hormonitor structure | **Preserved** — `AimiHormonitorStudyExporterMTR`, pattern export `physiological_patterns` in `DetermineBasalAIMI2` |
| Eversense | **Preserved** — fork `NotificationId` entries + plugin untouched in merge diff |
| DB maintenance | **Preserved** — `KeepAliveWorker` `runVacuum = false` |
| Graph empty data | **From dev** — applied in `BgGraphCompose` + `SecondaryGraphCompose` |

## AIMI vs upstream SMB / AutoISF

- **Alarm unification (dev):** Internal URGENT alarms use `AlarmSoundPlayer` + silent heads-up notification. AIMI `HYPO_RISK_ALARM` posts via `NotificationManager` with `NotificationId.HYPO_RISK_ALARM` (`URGENT`) — aligned with new tier. No AIMI logic change required beyond preserved enum entry.
- **AutoISF / OpenAPS SMB:** No functional changes in these 8 commits affecting AIMI decision path. Config Builder still selects APS plugin independently.
- **Legacy overview `NotificationStore`:** Fork-only; updated `UiInteractionImpl` to new alarm API so classic overview skin sound alarms still compile and route through `AlarmSoundPlayer`.

## Documentation cross-check

- [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) — latest merge log updated; §2 gates reviewed against this diff.
- [MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md) — merge section appended.
- [ALARM_UNIFICATION_PLAN.md](../ALARM_UNIFICATION_PLAN.md) — upstream doc added by merge; fork `UiInteractionImpl` adapted per Phase 2 pattern.
- AIMI docs (`AIMI_RECURSIVE_BELIEF.md`, `AIMI_PHYSIOLOGICAL_PATTERN_CATALOG.md`) — no upstream conflict; no content change required for this merge.

## User verification (required before release)

Follow [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) §4 smoke matrix:

- Dashboard ↔ classic overview skin toggle
- Graph with no recent BG (empty chart, no crash)
- AIMI loop + hypo alarm sound/snooze
- Adaptive smoothing with linear calibration
- JSON/CSV export + Health Connect permissions
- Hormonitor JSONL export on loop tick

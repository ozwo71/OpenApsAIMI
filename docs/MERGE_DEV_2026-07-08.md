# Merge log — `dev` → `dev_OAPSAIMI` (2026-07-08)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target
- **Target:** `dev_OAPSAIMI` (at `f2d833228d`).
- **Source:** `dev` @ `40275ce700` (= `milos/dev`, up to date; `dev...milos/dev` = 0/0).
- **4 incoming commits.** Working tree clean before merge.

## Scope of incoming commits (trivial, low-risk)
- `bb17f6bad0` Wear: press-and-hold repeat on wizard result correction buttons (`wear/.../AcceptActivity.kt`).
- `ad3944e161` dependabot: bump dagger 2.60 → 2.60.1 (`gradle/libs.versions.toml`).
- 2 merge commits (PR #4955, #4956).

Only **2 production files** touched: `gradle/libs.versions.toml` (dagger version) and
`wear/.../AcceptActivity.kt` (Wear UI).

- **No changes to AIMI / SMB / autoISF / determine_basal** (0 files under `openAPSAIMI`/`openAPSSMB`/autoISF).
  → **Nothing to port into AIMI.**
- **No Eversense files** (`settings.gradle`, `SourceSensor`, `PluginsListModule`, DI untouched) →
  Eversense constraint satisfied without re-application.
- **No `AppRepository` / `KeepAliveWorker` / `AndroidManifest` / DB VACUUM path** in the incoming diff
  → DB and physio-manifest gates preserved by construction.
- **No AIMI `docs/*.md`** touched by the merge.

## Conflicts
**Zero.** `gradle/libs.versions.toml` auto-merged cleanly (dagger → 2.60.1 kept alongside all fork lib
entries). Everything else fast-forwarded / auto-merged.

## Fork preservation verified (post-merge tree)
- dagger = `2.60.1` in `libs.versions.toml` (bump applied).
- AIMI: hormonitor export in `DetermineBasalAIMI2` (×4 `loop_blackbox_v1` / `AIMI_Decisions`); recent
  fork work intact — prebolus **one-shot latch** (`legacyPrebolusLatchBlocks` / `legacyPrebolusFiredAtMem`),
  concentration zero-bolus guard (`PumpWithConcentrationImpl` `result <= 0.0`).
- Eversense: `EVERSENSE_E3`/`EVERSENSE_365` in `SourceSensor` (×2).
- Dashboard skin switch: `dashboardOverview` in `MainScreen.kt`.
- Adaptive smoothing: `calibratedOrValue` in `ExponentialSmoothingPlugin` (×6).
- DB: `KeepAliveWorker.databaseCleanup` → `runVacuum = false`.
- ML storage / physio HC manifest / hormonitor structure: untouched by merge diff.

## Build / test
- `./gradlew :app:compileFullDebugKotlin` (full flavor, all modules, incl. dagger 2.60.1) — **BUILD SUCCESSFUL**.
- `PumpWithConcentrationImplTest` (incl. new U200 zero-floor guard) + AIMI unit tests — **PASS**.

## Pending (user, on-device — NON_REGRESSION §4)
- Smoke: dashboard ↔ original overview skin; AIMI + adaptive smoothie paths; JSON/CSV writes;
  xDrip/Dexcom inbox; Wear wizard-correction press-and-hold.

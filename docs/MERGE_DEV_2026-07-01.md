# Merge log — `dev` → `dev_OAPSAIMI` (2026-07-01)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target
- **Target:** `dev_OAPSAIMI` (at `00f75b11f3`, appVersion `4.0.0.0-dev.AIMI.010726`).
- **Source:** local `dev` @ `79c3d7bdce` (= `milos/dev`, fresh; behind by 0).
- **28 incoming commits.** Working tree clean before merge.

## Scope of incoming commits (low-risk for the fork)
Tests, Tidepool (don't switch modes / fix TBRs), SetupWizard Master-Client, QuickWizard improvements,
client-master UI visibility + remote-control searchable + SiteRotation client control, Statistics
expand-by-default, Dana bolus-block notification, insulin **concentration** fixes, Profile-edit save
fix, PS schedule-at-end, Gradle cleanup, junit 6.1.1, dagger.

- **No changes to AIMI / SMB / autoISF / determine_basal algorithms** (only test files under
  `plugins/aps/`). → **Nothing to port into AIMI.**
- **No changes to Eversense** (`settings.gradle`, `SourceSensor`, `PluginsListModule`, DI untouched)
  → Eversense constraint satisfied without re-application.
- **No changes to** `AppRepository` / `KeepAliveWorker` / `AndroidManifest` / DB VACUUM path in the
  incoming diff → DB and physio manifest gates preserved by construction.

## Conflicts (4, none Eversense; combine, not theirs-only)
1. **`core/keys/BooleanKey.kt`** — kept fork Eversense keys (`EversenseCloudUploadEnabled/Toast`) **and**
   took upstream's `SiteRotationManagePump/Cgm` with the added `SyncSpec(Cold, Bidirectional)`.
2. **`core/keys/IntKey.kt`** — took upstream's enriched `SiteRotationUserProfile`
   (`PreferenceType.LIST` + `entries` man/woman/child + `SyncSpec`) **and** kept all fork AIMI int keys
   (Garmin port, `OApsAIMI*`, `Aimi*` auditor / SOS / cosine / endo).
3. **`app/MainApp.kt`** — kept **both** `@Inject storageHelper: AimiStorageHelper` (fork ML storage)
   **and** upstream `@Inject profileSwitchExpiryScheduler`.
4. **`ui/.../runningMode/RunningModeManagementViewModel.kt`** — took upstream (adds
   `FailureReason.ControlDisabled` + `prepared.reason.failTextResId()`); no fork logic on this path.

All other overlapping files auto-merged cleanly (incl. `OverviewScreen*.kt`, `strings.xml`,
`DataHandlerMobile.kt`, `SafetyPlugin.kt`, `NotificationId.kt`, `ElementType.kt`).

## Fork preservation verified (post-merge tree)
- AIMI: new contexts (SlowCarbMeal / HypoRecovery) + effort keys present; hormonitor export in
  `DetermineBasalAIMI2` (×4 `loop_blackbox_v1` / `AIMI_Decisions`) intact.
- Eversense: `EVERSENSE_E3/365` in `SourceSensor` (×2); `:plugins:eversense` in `settings.gradle`.
- Dashboard skin switch: `dashboardOverview` in `MainScreen.kt` + `ComposeMainActivity.kt`.
- ML storage: `storageHelper: AimiStorageHelper` in `MainApp`.
- Adaptive smoothing: `calibratedOrValue` in `ExponentialSmoothingPlugin`.
- DB: `KeepAliveWorker.databaseCleanup` → `runVacuum = false`; `AppRepository` runVacuum guard intact.
- Physio: `AndroidManifest` Health Connect entries intact.

## Build / test
- `./gradlew :app:compileFullDebugKotlin` — **BUILD SUCCESSFUL** (full flavor, all modules).
- `:plugins:aps:testFullDebugUnitTest` `openAPSAIMI.context.* / activity.* / patient.*` — **PASS**.

## Pending (user, on-device — NON_REGRESSION §4)
- Smoke: dashboard ↔ original overview skin switch; AIMI + adaptive smoothie enabled paths; JSON/CSV
  writes; xDrip/Dexcom inbox no stall; low-end stability mode ON/OFF.

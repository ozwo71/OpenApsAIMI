# Merge log — `milos/dev` → `feature/dexcom-oneplus-native` (2026-07-31)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md),
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md), and
[MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md](MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md).

## Source / target

- **Target:** `feature/dexcom-oneplus-native` (pre-merge `2785f1d244`).
- **Source:** `milos/dev` @ `88d31b816d` (CI fix; ~106 commits since merge-base).
- **Merge-base:** `638f23dfab` (previous documented merge 2026-07-16).
- Working tree was clean before merge.

## Scope of incoming commits (high level)

| Area | Summary |
|------|---------|
| APS API | `supportsDynamicIsf` → `usingDynamicIsf()` + `offersDynamicSensitivity()`; Autosens visibility via `VisibilityContext.apsOffersDynamicSensitivity` |
| SMB / AutoISF | Abort guards on invalid ISF / `carb_ratio` / autosens inputs (AIMI already had parity) |
| Insulin | `Insulin` interface removed; `InsulinManager` + `profile.iCfg` / `runningICfg` are the source of truth |
| Overview graphs | Dynamic Y-scale helpers + `VisibleRangeHolder` (pinch-zoom safe) |
| Wear | `watchfacepush` module; DataHandlerMobile insulin-manager path |
| Prefs Compose | `TextWithSyncBadge` / `PreferenceTitleWithSyncBadge` API for Adaptive* prefs |
| CI / tooling | CircleCI + aaps-ci updates |

## Conflicts — decisions (not theirs-only on fork paths)

| File / area | Resolution |
|-------------|------------|
| `Versions.kt` | **Ours** AIMI suffix → `4.0.0.0-dev.AIMI.310726` |
| `NotificationId` | **Combine** — keep `AIMI_AUDITOR_INSIGHT` + upstream `CARBS_STORE_FAILED` |
| Adaptive* prefs | **Combine** then **fix** to `PreferenceTitleWithSyncBadge` (new SyncBadge Boolean API) |
| `AllPreferencesScreen` / `AppNavGraph` | **Combine** `configBuilder` + `onOpenLegacyXmlPreferences` |
| `BuiltInSearchables` | **Combine** insulin manager + skin descriptions + `profileFunction`; drop dead `Insulin` ctor |
| Insulin management | **Ours** Afrezza inhaled peak limits |
| `BgGraphCompose` / `GraphsSection` | **Ours** dashboard graph params |
| `GraphUtils` | **Combine** dashboard TBR lane + upstream `VisibleRangeHolder` |
| CircleCI / aaps-ci | CircleCI theirs; aaps-ci keep fork modes |
| `settings.gradle` | Auto-merged: `eversense`, `dexcom_oneplus`, `libkeks`, `wear:watchfacepush` |
| `values-ro-rRO/strings.xml` | Deduped merge duplicate string names |

## AIMI / APS parity ported after merge

| Change | File | Notes |
|--------|------|-------|
| `usingDynamicIsf()` + `offersDynamicSensitivity() = true` | `OpenAPSAIMIPlugin.kt` | Match SMB; Autosens pref visibility |
| Peak / concentration from profile | `OpenAPSAIMIPlugin.kt` | `insulin.iCfg` → `profile.iCfg` (⚠️ Insulin DI removed upstream) |
| Invalid ISF abort | already present on AIMI | Parity with SMB/AutoISF carb_ratio / autosens guards |

**Not ported (deferred, no AIMI regression):** overview dynamic BG Y-scale into dashboard Vico graphs — would need a dedicated dashboard-safe port so skin switch / dashboard TBR lane stay intact.

**AutoISF:** upstream still has no `offersDynamicSensitivity()` override (defaults false) — left aligned with `milos/dev`.

## Fork preservation verified (post-merge tree)

- **AIMI:** plugin DI + dynISF API + T3C/CFRD prefs path intact.
- **Adaptive smoothie:** `calibratedOrValue` usages preserved; `AdaptiveSmoothingPluginTest` 6/6 PASS.
- **Dashboard / skins:** `dashboardOverview` in `ComposeMainActivity` / `MainScreen`; skin list via `SkinDescriptionProvider` in prefs (user can return to overview skin).
- **ML / physio permissions:** `MANAGE_EXTERNAL_STORAGE` + Health Connect AIMI activities in `AndroidManifest.xml`.
- **Hormonitor:** `patient_story` export / schema ≥ 1.2.0 paths intact in exporter + viewer.
- **DB freeze gate:** `KeepAliveWorker` → `runVacuum = false`; `AppRepository.cleanupDatabase` vacuum only when requested.
- **Eversense:** `:plugins:eversense` included; `SourceSensor` E3/365 present.
- **Dexcom ONE+:** `:plugins:dexcom_oneplus` included; `SourceSensor.DEXCOM_ONEPLUS_NATIVE` present.

## Validation run (this merge)

| Gate | Result |
|------|--------|
| `:app:compileFullDebugKotlin` | **PASS** |
| AIMI `SmbDampingTest` / `SmbDampingUsecaseTest` / `UnifiedReactivityLearnerLearnDynamicsTest` | **PASS** |
| AIMI `T3cCfrdCorrectifsReplaySimulationTest` | **PASS** (7) |
| `AdaptiveSmoothingPluginTest` | **PASS** (6) |
| On-device smoke (cold start, skin switch, ONE+, Eversense, ML write) | **Pending user confirmation** — not marked working |

## Notes for follow-up

1. Port upstream overview dynamic scaling into dashboard graphs without dropping TBR lane / skin switch.
2. Device smoke per checklist §4 (dashboard skin toggle, AIMI JSON/CSV write, physio Health Connect, ONE+/Eversense listing).
3. Optional: AutoISF `offersDynamicSensitivity()` if upstream later adds it — keep AIMI/SMB in lockstep.

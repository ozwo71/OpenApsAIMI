# Merge log — dev @ `b45fb221e8` → `dev_OAPSAIMI` (2026-07-07)

Merge commit: `14856bddcd` (`git merge milos/dev`, ort strategy, **zero conflicts**).

## Upstream scope (12 commits since `2e09122620`)

| Area | Commits | Fork impact |
|------|---------|-------------|
| Wear scene tiles | Scenes tile settings screen, Auto/None slot distinction, unified color themes, carb color | None (pure upstream additions; `wear` compiles) |
| TT rounding fix `f13d7e439a` | `Profile.getRoundedTargetMgdl()` + `apsAdjustedTargetMgdl()` extension; migrated `PersistentNotificationPlugin`, `DataHandlerMobile`, `OverviewDataCacheImpl`, `WidgetStateLoader` | **Ported to fork-only file** (see below) |
| BgQuality LGS `b45fb221e8` | `BgQualityCheckPlugin.isLgsForced()` → surfaces doubled-BG maxIOB=0 as `CLOSED_LOOP_LGS` mode | None — constraint hook existed; only the BgQualityCheck override is new. No AIMI/SMB/autoISF override needed |
| Tidepool upload ID | `TidepoolUploader` | None |
| Equil | max basal fix, TBR result fix, BLE | None (auto-merged with fork `cancelPendingConnect`) |
| Tests | UKF smoothing test, Diaconn hashtable tests, ProfileExtension test, BgQualityCheck test | Adapted to fork API (see below) |

## Non-regression constraint review (per NON_REGRESSION_CHECKLIST.md)

- **AIMI**: `plugins/aps` untouched by the merge diff (0 files). Hormonitor export intact in `DetermineBasalAIMI2`. AIMI unit tests PASS.
- **Adaptive Smoothie**: `AdaptiveSmoothingPlugin` / `UnscentedKalmanFilterPlugin` untouched; `calibratedOrValue` verified in both.
- **Dashboard + skin**: `MainScreen.kt` untouched; `dashboardOverview` skin switch intact (original overview reachable).
- **ML permissions / storage**: `AndroidManifest.xml` (app) untouched; `MainApp` storageHelper untouched.
- **Physio**: no physio file in upstream diff.
- **Hormonitor structure**: no export/schema file touched.
- **Eversense**: `settings.gradle` `:plugins:eversense` intact; `SourceSensor` EVERSENSE / E3 / 365 intact; no DI/registration file in diff.
- **DB vacuum gates**: `KeepAliveWorker` → `runVacuum = false` intact; `AppRepository.cleanupDatabase` guard intact.

## Fork adaptations (post-merge commits)

1. **`OverviewFragment.kt` (fork-only legacy overview, empty upstream)** — ported the TT rounding fix:
   old unrounded `targetUsed`/`abs(getTargetMgdl() - targetUsed)` compare replaced by
   `profile.apsAdjustedTargetMgdl(loop, config, processedDeviceStatusData)`. Without this, mmol
   profiles would flag every loop run as "adjusted target" in the classic overview ribbon
   (the compose overview + widget + wear + persistent notification were migrated upstream).
2. **`UnscentedKalmanFilterPluginTest.kt` (new upstream test)** — bridged to the fork's
   `suspend fun smooth(data, context)` signature via `runBlocking` helper (parity with
   `AdaptiveSmoothingPluginTest`).
3. **Pre-existing fork test drift fixed (blocked the constraints test task):**
   - `ConstraintsCheckerImplTest` / `SafetyPluginTest`: `AutosensDataStore` import moved to
     `core.interfaces.aps`; `isAdvancedFilteringEnabled` is now suspend → `runTest`; stub
     `persistenceLayer.isAdvancedFilteringSupported()` (SafetyPlugin no longer reads
     `autosensDataStore.lastBg()` for this).
   - `ObjectivesViewModelTest`: `ObjectivesViewModel` ctor gained `config` param.

## Validation

- `:app:compileFullDebugKotlin` + `:wear:compileFullDebugKotlin` — **PASS**.
- `:plugins:smoothing` + `:core:objects` + `:plugins:constraints` unit tests — **PASS**.
- `:plugins:aps` (AIMI) unit tests — 192/200 suites PASS. **8 failing suites are pre-existing, NOT
  caused by this merge** (verified: identical failure signatures reproduced at the pre-merge commit
  `ad0e53c908` in an isolated worktree). Two root causes, both fork test-env drift:
  - `AimiHormonitorStudyExporterMTR.<init>:223` NPE under JVM unit tests
    (`Environment.getExternalStorageDirectory()` returns null off-device) → scenario tests abort in
    bootstrap (`DetermineBasalAimi*ScenarioTest`, `DetermineBasalAimiPerInvocationCacheTest`).
  - `DetermineBasalInvocationCachesTest` TDD cache expectations drifted from implementation.
  - Fork-critical suites PASS: `LegacyPrebolusLatchTest`, `LegacyPrebolusMissedDeliveryTest`,
    `UndeclaredCobEstimatorTest`.
- Device smoke matrix (checklist §4) — to be run by user before release.

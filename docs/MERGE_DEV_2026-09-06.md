# Merge log — `dev` → `dev_OAPSAIMI` (2026-09-06)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md),
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md), and
[MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md](MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md).

## Source / target

- **Target:** `dev_OAPSAIMI` (pre-merge `b6744f7a62`, safety tag `premerge-dev-20260906`).
- **Source:** `dev` @ `283a184f60` — re-fetched from `milos/dev` before merging; local `dev` was already
  identical (upstream has not moved since 2026-08-25).
- **Merge-base:** `7fc8205e9a`.
- **Incoming:** 22 commits. Working tree clean before merge.

## Scope of incoming commits

Upstream touched **6 files only** — build tooling plus one Wear manifest fix. No source change at all.

| Area | Change |
|------|--------|
| Gradle | wrapper 9.6.1 → **9.7.1** (`gradle-wrapper.jar`, `.properties`, `gradlew`, `gradlew.bat`) |
| AGP | `gradlePlugin` 9.3.1 → **9.3.2** |
| Compose | `composeBom` 2026.06.01 → **2026.08.00** |
| Charts | **Vico 3.2.3 → 3.3.0** (used by the fork dashboard graphs — main risk of this merge) |
| Libs | okhttp 5.4.0 → 5.5.0, appcompat 1.7.1 → 1.8.0, firebase-bom 34.17.0 → 34.18.0, java-otp 0.4.0 → 1.0.0, junit 6.1.2 → 6.1.3 |
| Wear | `uses-library wear-sdk android:required="false"` — fixes `INSTALL_FAILED_MISSING_SHARED_LIBRARY` on Wear OS 3 |

## Conflicts

**None.** Two files auto-merged (`gradle/libs.versions.toml`, `wear/src/main/AndroidManifest.xml`); both were
reviewed by hand rather than trusted, because the 2026-08-03 merge showed that a clean auto-merge of a
manifest or a resource file can still produce a build-breaking duplicate.

| Auto-merged file | Verification |
|------------------|--------------|
| `gradle/libs.versions.toml` | Delta is **only** the version bumps listed above — no fork entry dropped. Fork ML deps live in `plugins/aps/build.gradle.kts` (TFLite 2.4.0, ONNX for the OREF advisor) and were untouched. |
| `wear/src/main/AndroidManifest.xml` | Merge delta vs pre-merge is **empty** — the fork already carried the `wear-sdk` optional fix, so nothing was lost and nothing was duplicated. `BgGraphComplication` still declared exactly **once** (the 2026-08-03 dedup holds). |

## AIMI / SMB / AutoISF parity

**Nothing to port.** The upstream diff contains no `openAPS*`, `determine_basal`, SMB, AutoISF, smoothing,
dashboard, `SourceSensor`, DI, DB or storage file — verified mechanically:

```
git diff --name-only 7fc8205e9a..dev | grep -iE "aimi|smooth|hormonitor|dashboard|Skin|SourceSensor|eversense|dexcom|KeepAlive|AppRepository|determine|openAPS"
→ (empty)
```

The pre-existing AIMI-vs-SMB `invalidInputs` drift documented in
[MERGE_DEV_2026-08-03.md](MERGE_DEV_2026-08-03.md) is unchanged and still open.

## Fork preservation verified

Invariant script run before and after the merge — output **byte-identical**
(`diff base_pre.txt base_post.txt` → no differences):

| Gate | Value (unchanged) |
|------|-------------------|
| AIMI | 449 plugin files, `usingDynamicIsf`, `offersDynamicSensitivity`, `invalidInputs` guard, `@IntKey(225)` |
| Adaptive smoothie | 23 `calibratedOrValue` usages |
| Dashboard + skin switch | 5 `dashboardOverview`, 9 `SkinDescriptionProvider` |
| ML / physio permissions | 14 matching lines in `app/src/main/AndroidManifest.xml` (storage, Health Connect, `ACTIVITY_RECOGNITION`, AIMI HC activity, FGS `dataSync`) |
| Hormonitor | 7 `patient_story`, 14 hormonitor `.kt` files |
| DB freeze gate | `KeepAliveWorker` `runVacuum = false`; `AppRepository` vacuum only on request |
| DataInbox | 6 `drain()` call sites |
| Eversense / ONE+ | modules included; `SourceSensor` E3/365 + `DEXCOM_ONEPLUS_NATIVE`; `@IntKey(445)`/`(446)`; notification-reader mappings |
| Libre 3 native | `:plugins:libre3` included; `SourceSensor.LIBRE_3_NATIVE`; `@IntKey(447)` in `SourcePluginsListModule`; `NotificationId.LIBRE3_DIR_ACCESS_LOST` still the **last** enum entry; follower `LIBRE_2` / `LIBRE_2_NATIVE` / `LIBRE_3` untouched |
| Duplicate scan | **clean** — no duplicate resource name, no duplicate manifest component |

## Validation run

| Gate | Result |
|------|--------|
| Duplicate resource / manifest scan (new §1 gate) | **PASS** — clean |
| `:app:assembleFullDebug` + `:wear:assembleFullDebug` (what CI builds) | **PASS** — BUILD SUCCESSFUL, both APKs produced (Vico 3.3.0 / compose-bom 2026.08 / AGP 9.3.2 / Gradle 9.7.1 all compile) |
| `:plugins:aps` unit tests (incl. AIMI) | **PASS** — 1677 tests, 0 failures |
| `:plugins:smoothing` unit tests | **PASS** — 14 tests, 0 failures |
| On-device smoke | **Pending user confirmation** — not marked working |

## Notes for follow-up

1. **Vico 3.3.0 renders untested.** The dashboard graphs compile and the unit tests pass, but chart
   rendering is not covered by tests — check the dashboard graphs visually on device (BG curve, TBR lane,
   predictions) as part of the smoke.
2. Wear OS 3 install fix now applies to release builds too — worth confirming on an older watch if one is
   available.
3. Still open from 2026-08-03: AIMI `invalidInputs` TDD check decision, the 17 pre-existing
   `:implementation` test failures, and the `:ui` `GraphViewModelTest` constructor mismatch.

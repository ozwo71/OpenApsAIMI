# Merge log — `dev` (milos) → `dev_OAPSAIMI` (2026-08-08)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md),
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md), and
[MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md](MERGE_CONSTRAINT_DEXCOM_ONEPLUS.md).

## Source / target

- **Target:** `dev_OAPSAIMI` (pre-merge `aa5538c487`, safety tag `premerge-dev-20260808`).
- **Source:** local `dev` @ `7fc8205e9a` (tracks `milos/dev`, fetched 2026-08-08, `dev` == `milos/dev`).
- **Merge-base:** `fa2d2c78a5` (previous documented merge 2026-08-03).
- **Incoming:** 24 commits, 269 files. Working tree was clean before the merge.

## Scope of incoming commits (high level)

| Area | Summary |
|------|---------|
| **APS safety (SMB / AutoISF / AMA)** | Non-finite hardening: `round()` passes NaN **and ±Infinity** through instead of letting `Math.round` saturate; `setTempBasal` falls back to the profile basal on a non-finite rate; new `abortNonFinite()` replaces two uncaught `throw`s (`remainingCIpeak`, `remainingCI`); aborts on non-finite `eventualBG` / `minGuardBG`; `fractionCarbsLeft` guards `mealCOB / 0`; prediction clamps use `coerceAtLeastFinite` instead of `max()` (which propagates NaN) |
| **HardLimits API** | Arrays/constants → `AgeType`-keyed maps and ranges: `MIN_DIA`/`MAX_DIA` → `LIMIT_DIA`, `MIN_IC`/`MAX_IC` → `LIMIT_IC`, `MIN_ISF`/`MAX_ISF` → `LIMIT_ISF`, `MIN_PEAK`/`MAX_PEAK` → `LIMIT_PEAK`, `LIMIT_TEMP_*_BG` `doubleArrayOf` → ranges; accessors `minDia()`/`maxDia()`/`minPeak()`/`maxPeak()`/`minIC()`/`maxIC()` → `diaRange()`/`peakRange()`/`icRange()`; `isInRange()` removed |
| **Constants** | Naming + ranges: `defaultDIA`→`DEFAULT_DIA`, `notificationID`→`NOTIFICATION_ID`, `remoteBolusMinDistance`→`REMOTE_BOLUS_MIN_DISTANCE`, `dailyLimitWarning`→`DAILY_RESERVOIR_LIMIT_WARNING`, CPP min/max → `CPP_PERCENTAGE_RANGE`/`CPP_TIMESHIFT_RANGE`, `MAX_PROFILE_SWITCH_DURATION`→`ACTION_DURATION`, `MIN/MAX_TT_*`→`TT_RANGE_MGDL`/`TT_RANGE_MMOL`; `MAX_LOG_LINES`, `SMALL_WIDTH`, `SMALL_HEIGHT`, `DEFAULT_PROFILE_ARRAY` removed |
| **Number formatting** | New pure-Kotlin `core:data` `NumberFormat` / `NumberFormatPlatform` replaces `java.text.DecimalFormat` in API signatures (`DecimalFormatter.pumpSupportedBolusFormat`) and Compose parameters |
| **Sensitivity** | `detectSensitivity()` takes `profile`, `siteChanges`, `profileSwitches` from the caller (read once per detection start instead of per bucketed point); `PrepareGraphDataWorker` hoists the three reads |
| **Profile sync** | `ProfileRepository.reset()` after `doMigrations()`; large `ProfileRepositoryImpl` rework + tests |
| **Misc** | `TimeUnit` → `kotlin.time.Duration`, `:core:keys` JVM dependency removed, `ComposedKey` rework, Tidepool network-error recovery, TT limit fixes, scene expiration fix, `Percentile` DoubleArray overload, new tests |

## Conflicts — decisions (10, all "combine", no theirs-only on fork paths)

| File | Resolution |
|------|------------|
| `app/.../MainApp.kt` (2 hunks) | **Combine** — kept both imports (`android.provider.Settings` + `kotlin.time…milliseconds`); kept the fork's `maintainDatabaseIfDue()` (**no startup VACUUM** — DB freeze gate) **and** added upstream's `profileRepository.reset()`. Upstream's `vacuumDatabaseIfDue()` deliberately **not** taken. |
| `core/data/.../Constants.kt` | **Combine** — took upstream's range block, but kept the **fork's 300 %** profile-switch cap: `CPP_PERCENTAGE_RANGE = 30.0..300.0` (upstream 250.0). Fork commit `34d0fb34f8` "increase % switch profile". |
| `core/interfaces/.../HardLimits.kt` | **Combine** — took upstream's map/range API, re-expressed **every fork widening** in it: `LIMIT_DIA` bottom 4.0 from ADULT up and PREGNANT top 12.0 (upstream 5.0 / 10.0), `LIMIT_ISF = 2.0..1200.0` (upstream 1000.0). Added the fork-only inhaled-insulin limits in the same shape: `LIMIT_DIA_INHALED` (1.5..4.0), `LIMIT_PEAK_INHALED` (20..45) + accessors `diaRangeInhaled()` / `peakRangeInhaled()`. Accepted upstream's `LIMIT_TEMP_MIN_BG` top 180.0 → **180.16** (documented: a 10.0 mmol temp target). |
| `implementation/.../HardLimitsImpl.kt` | **Combine** — upstream `getValue(loadAge())` implementations + the two fork inhaled accessors. |
| `shared/tests/.../HardLimitsMock.kt` | **Combine** — same, plus dropped the fork's duplicate `loadAge()` (upstream's `AgeType`-based one is already in the file). |
| `core/objects/.../ProfileSealed.kt` | **Combine** — kept the fork's inhaled-DIA heuristic in `validateSemantic`, rewritten on the range API (`isInRange()` no longer exists upstream). |
| `ui/.../InsulinManagementViewModel.kt` (2 hunks) | **Combine** — kept the fork's inhaled branches in `saveCurrentInsulin`, `diaRange()`, `peakRange()` and the whole fork-only `resolveEditorTemplate()`, all on the range API. |
| `core/ui/.../AdaptiveDoublePreference.kt` (2 hunks) | **Combine** — kept the fork's span-adaptive `decimalPlaces`/`step` logic, switched the formatter to upstream `NumberFormat.withDecimals(decimalPlaces)` (equivalent for 0–3 decimals). |
| `wear/.../StepCountListener.kt` (4 hunks) | **Combine** — kept the fork's `synchronized(stepsMap)` blocks (physio/step path, concurrent sensor callbacks) with upstream's `contains` → `containsKey`. |
| `workflow/.../PrepareGraphDataWorker.kt` (3 hunks) | **Combine** — kept the fork's warm-start `AppInitCalculationPolicy.warmStartOldestBucketIndex` bucket skipping in **both** loops **and** added upstream's three hoisted `detectSensitivity` inputs; kept both imports. |

### Transitive fixes (not conflicts, the build would have failed)

| File | Fix |
|------|-----|
| `plugins/main/.../overview/OverviewFragment.kt` | Fork-only legacy overview (**the original skin**). Upstream deleted `Constants.SMALL_WIDTH` / `SMALL_HEIGHT`; moved them to a private `companion object` in the fragment. `Constants.notificationID` → `NOTIFICATION_ID`. |
| `plugins/sync/.../DataHandlerMobile.kt` | `findAfrezzaIcfg()` rewritten on the new API: `HardLimits.LIMIT_PEAK_INHALED`, `LIMIT_DIA.values.minOf { it.start }`, `LIMIT_DIA_INHALED`. |
| `plugins/aps/.../openAPSAIMI/compose/PkpdSettingsUi.kt` | `java.text.DecimalFormat` → `NumberFormat.withDecimals()` (`SliderWithButtons.valueFormat` is now `NumberFormat`). |
| `plugins/aps/src/test/.../basal/BasalPlannerTest.kt` | `DummyFormatter.pumpSupportedBolusFormat` return type `DecimalFormat` → `NumberFormat`. |
| `plugins/source/src/test/.../NotificationCollectorServiceTest.kt` | **New upstream test** adapted to the fork's value-aware `GlucoseDeduplicator.process(packageName, now, glucoseMgdl)` (3 args; upstream has 2). Fork feature kept, test adapted. |
| `implementation/src/test/.../HardLimitsImplTest.kt` | Realigned `diaRange` expectations to the fork values (adult `4.0..9.0`, pregnant `4.0..12.0`) and added a test for `diaRangeInhaled()` / `peakRangeInhaled()`. **Clears 2 of the 17 pre-existing `:implementation` failures.** |

## AIMI / APS parity review (upstream → AIMI)

Upstream hardened `DetermineBasalSMB`, `DetermineBasalAutoISF` and `DetermineBasalAMA` against
non-finite values. Reviewed point by point against `DetermineBasalAIMI2` / `OpenAPSAIMIPlugin`:

| Upstream change | AIMI status | Action |
|-----------------|-------------|--------|
| `round(value, digits)`: `isNaN` → `!isFinite` | AIMI had the NaN-only guard, so `Math.round` saturated ±Infinity into a normal-looking number and blinded every downstream `isFinite()` check | **Ported** to `DetermineBasalAIMI2.round(Double, Int)` |
| `round(value): Int`: `isNaN` → `!isFinite` + `roundNonFinite=$value` token | Same NaN-only gap | **Ported** (token renamed so the `PersistenceLayerImpl` tripwire still matches) |
| `setTempBasal`: non-finite rate → profile basal | AIMI's `setTempBasal` is fork-specific but ends in `rT.rate` through `coerceIn`, which lets NaN pass | **Ported** — sanitized `requestedRate` computed once at the top of the function; all four `_rate` uses routed through it |
| `hardLimits` range API in the plugin invoke path | AIMI used the old accessors / `LIMIT_*[0]`,`[1]` indices | **Ported** — `diaRange()`, `icRange()`, `HardLimits.LIMIT_ISF`, `LIMIT_MIN_BG`, `LIMIT_MAX_BG`, `LIMIT_TARGET_BG`, `LIMIT_TEMP_*_BG` (parity with `OpenAPSSMBPlugin`) |
| `abortNonFinite()` on `remainingCIpeak` / `remainingCI` (were uncaught `throw`) | AIMI never throws there — `remainingCIpeak` is only used in a console line, not in dosing | **Not needed** |
| `abortNonFinite()` on `eventualBG` / `minGuardBG` | AIMI's `eventualBG` comes from `intsPredictions.last()`, an `Int` list, so it is finite by construction | **Not needed** |
| `coerceAtLeastFinite` in the `[39, 401]` prediction clamps | AIMI's `sanitizeCurve()` already drops NaN (`mapNotNull`) and the `min/max` pair bounds ±Infinity to 401 / 39 | **Not needed** |
| `fractionCarbsLeft` = `mealCOB / carbs` zero guard | AIMI has no `fractionCarbsLeft` | **Not applicable** |
| `Double.withoutZeros()` / `toFixed2()` → `NumberFormat` | Cosmetic only; AIMI's local `java.text.DecimalFormat` still compiles and formats identically | **Declined** (no behaviour delta, avoids churn in a 16k-line file) |
| `Sensitivity.detectSensitivity()` 3 new params | AIMI implements no `Sensitivity` plugin; only `PrepareGraphDataWorker` calls it | **None needed** |

**Behaviour deltas accepted in AIMI** (identical to SMB/AutoISF, so no divergence):

- temp-target low limit top `180.0` → `180.16` mg/dL, so a 10.0 mmol temp target is no longer clamped
  and no longer warns on every run.

### `invalidInputs` divergence — resolved (follow-up to 2026-08-03)

The 2026-08-03 log left the `OpenAPSSMBPlugin` ↔ `OpenAPSAIMIPlugin` `invalidInputs` gap open, and
concluded that "only the `TDD` check and the abort observability are actually missing". Re-read
against the post-merge code, **that conclusion was wrong in an important way**: the risk is not a
zero or missing TDD, it is a **NaN** one.

- `TDD <= 0.0` **cannot happen in AIMI**. Unlike SMB, AIMI always has a TDD: it falls back to the
  `OApsAIMITDD7` preference (declared `40.0, min 1.0, max 150.0`). Adding that half of the check
  would be dead code, and gating it on the raw `dynIsfMode` (no `tddPartsCalculated()` equivalent
  exists in AIMI) would have been the wrong fix for a problem that does not exist.
- `insulinDivisor <= 0` is equally dead: AIMI's divisor is a `when` on the peak returning 45/55/65.
- **The real hole is NaN.** The four floors that build AIMI's TDD all test `== 0.0` / `< tdd7P`, and
  every comparison with NaN is false, so a NaN passes all four untouched:
  `OpenAPSAIMIPlugin` lines ~1008 (`tdd2Days`), ~1018 (`tddDaily`), ~1026 (`tdd24Hrs`) and
  `DetermineBasalAIMI2` ~1677 (`tdd7Days`). Downstream, the `tdd7Days.toFloat() != 0.0f` guards in
  `DetermineBasalAIMI2` are **true** for NaN, so it would enter those branches and make
  `basalaimi` (`tdd7Days / weight`) and `ci` (`450 / tdd7Days`) NaN for the whole tick.
- `variable_sens` is clamped by `coerceIn(5.0, 300.0)` just before use, so the `<= 0.0` half of its
  guard is dead — but `NaN.coerceIn(...)` returns NaN, so the `!isFinite()` half stays live and useful.

**Applied (this branch, uncommitted at the time of writing):**

| Fix | Where | Note |
|-----|-------|------|
| `!x.isFinite() ||` prepended to the four TDD floors | `OpenAPSAIMIPlugin` ×3, `DetermineBasalAIMI2` ×1 | Root fix. **No behaviour change for any finite value** — the existing `== 0.0` / `< tdd7P` conditions are untouched. |
| `!oapsProfile.TDD.isFinite()` added to `invalidInputs` | `OpenAPSAIMIPlugin` | Backstop only. **Not** gated on `dynIsfMode`, and **without** `TDD <= 0.0`, for the reasons above. |
| `fabricPrivacy.logException()` + `EventOpenAPSUpdateGui()` on abort | `OpenAPSAIMIPlugin` | Parity with SMB. `FabricPrivacy` added to the constructor (Dagger only — no manual construction or subclass of the plugin exists). Without these the abort never reached Crashlytics and the APS card kept showing the previous run. |

No AIMI dosing logic was changed. Validation: `:app:assembleFullDebug` + `:wear:assembleFullDebug`
**SUCCESSFUL**, `:plugins:aps` **1253 tests, 0 failures**.

## Recent AIMI work — impact check (explicit user request)

Cross-checked the 19 files touched by the last fork commits (`aa5538c487` shadow ISF exit clamp,
`2fcc0afa35` physiological ISF factor + Ra decay, `556580a886` physio ISF factor / ISF-chain telemetry,
`49cb6ae5dc` "Hypo treated" low-glucose alarm action) against the files the merge changed:

- **Only 2 overlap** — `DetermineBasalAIMI2.kt` and `OpenAPSAIMIPlugin.kt` — and in both the only
  changes are the parity ports listed above (verified with `git diff premerge-dev-20260808`).
- **BG alarms untouched.** `AlarmAction.kt`, `NotificationId.kt`, `LocalAlertUtils.kt`,
  `LocalAlertUtilsImpl.kt`, `AlarmNotificationManager.kt`, `HypoTreatedReceiver.kt` and the app
  `AndroidManifest.xml` receiver entry are **byte-identical** to pre-merge.
  `NotificationManagerImpl.kt` only received the `TimeUnit` → `Duration` migration
  (`validMinutes.minutes.inWholeMilliseconds`), which is behaviour-preserving.
- **Physio / ML / hormonitor / smoothing / dashboard:** no file on those paths is in the merge diff.

## Fork preservation verified (invariant baseline diff)

Full invariant script run before and after the merge — **outputs byte-identical, zero diff**
(not even line-number shifts).

| Gate | Evidence |
|------|----------|
| **AIMI** | 437 plugin files, `usingDynamicIsf` + `offersDynamicSensitivity`, `@IntKey(225)` + `bindOpenAPSAIMIPlugin` |
| **Adaptive smoothie** | 23 `calibratedOrValue` usages across the 5 same files (`AdaptiveSmoothingPlugin` 8, UKF 7, exponential 6, avg 1, `InMemoryGlucoseValue` 1) |
| **Dashboard + skin switch** | 5 `dashboardOverview` sites (`ComposeMainActivity` + `MainScreen`), 4 `SkinDescriptionProvider` files, `@IntKey(20)` `bindOverviewPlugin` → **the user can still switch back to the original Overview skin**; the legacy `OverviewFragment` also kept compiling (see transitive fix) |
| **ML / physio permissions** | `MANAGE_EXTERNAL_STORAGE`, `READ`/`WRITE_EXTERNAL_STORAGE`, `ACTIVITY_RECOGNITION`, `POST_NOTIFICATIONS`, FGS `dataSync`, 7 Health Connect `health.READ_*`, `AIMIHealthConnectPermissionActivityMTR` (+ alias) — all present, unchanged → JSON/CSV writes and physio reads keep their permissions |
| **Hormonitor** | 5 `patient_story` refs, hormonitor refs unchanged across the same 12 files (`DetermineBasalAIMI2` 26, exporter 15, viewer 21/17/51, …) |
| **DB freeze gate** | `KeepAliveWorker` → `cleanupDatabase(..., runVacuum = false)`; `AppRepository.cleanupDatabase` still `runVacuum: Boolean = false`, no inline VACUUM; **`MainApp` still uses `maintainDatabaseIfDue()`, upstream's `vacuumDatabaseIfDue()` refused** |
| **DataInbox** | 6 `drain()` sites unchanged (Dexcom, xDrip + their tests) |
| **Eversense** | `:plugins:eversense`, `SourceSensor` `EVERSENSE` / `EVERSENSE_E3` / `EVERSENSE_365`, `@IntKey(445)` + `bindEversensePlugin` |
| **Dexcom ONE+** | `:plugins:dexcom_oneplus` + `:plugins:libkeks`, `SourceSensor.DEXCOM_ONEPLUS_NATIVE`, `@IntKey(446)`, notification-reader v3 with `com.dexcom.d1plus` / `com.dexcom.dexcomone` |
| **Notification reader** | Senseonics + Dexcom rows and `"version": 3` unchanged; fork's value-aware `GlucoseDeduplicator` kept (upstream test adapted, not the code) |
| **Version** | `appVersion = "4.0.0.0-dev.AIMI.070726"` |
| **AIMI docs** | All **50** `.kt` references in `AIMI_ARCHITECTURE_MAP.md`, `AIMI_ROADMAP.md` and the merge-constraint docs resolve |

**Upstream file deleted by the merge:** `ALARM_UNIFICATION_PLAN.md` (repo root). This is an **upstream**
document — added by upstream `937d77b82b` "Fix and unify alarm system" and deleted by upstream
`bf647237e6`; the fork never edited it. The deletion is legitimate and does not touch the fork's
own alarm work.

## Validation run (this merge)

| Gate | Result |
|------|--------|
| Duplicate `strings.xml` names (repo-wide) | **Clean** |
| Duplicate manifest components (`app`, `wear`) | **Clean** |
| `:app:assembleFullDebug` + `:wear:assembleFullDebug` | **BUILD SUCCESSFUL** — both APKs produced |
| `:plugins:aps` (incl. AIMI) unit tests | **PASS** — 1253 tests, 0 failures |
| `:plugins:smoothing` unit tests | **PASS** |
| `:core:interfaces`, `:core:objects`, `:plugins:sensitivity`, `:plugins:constraints` | **PASS** |
| `:implementation` unit tests | 724 tests, **15 failed — all pre-existing** (was 17; the merge fixes 2) |
| `:plugins:source` unit tests | 280 tests, **18 failed — all pre-existing** |
| On-device smoke | **Pending user confirmation** — not marked working |

### Failure attribution (pre-merge worktree, as required by §1)

`git worktree add --detach <dir> premerge-dev-20260808`, `local.properties` copied, same tasks run:

| Module | Pre-merge | Post-merge | Verdict |
|--------|-----------|------------|---------|
| `:implementation` | 710 tests, 17 failed — `KeepAliveWorkerTest` 14, `HardLimitsImplTest` 2, `LastBgDataImplTest` 1 | 724 tests, 15 failed — `KeepAliveWorkerTest` 14, `LastBgDataImplTest` 1 | No regression; **`HardLimitsImplTest` now green** (test realigned to the fork DIA range) |
| `:plugins:source` | 241 tests, 18 failed — `GlunovoPluginTest` 9, `IntelligoPluginTest` 9 | 280 tests, 18 failed — identical set | No regression; the 39 extra tests are new upstream tests, all passing |

### Flaky (not regressions)

Two `:plugins:aps` tests failed once in a batch run and pass on re-run and on a full
`--rerun-tasks` pass:

- `aimiNeuralNetworkTest > test training reduces loss()` — unseeded random weight init, so a bad
  draw can leave the loss flat.
- `EngineBenchmarks > benchmark determine_basal execution time()` — wall-clock benchmark, sensitive
  to machine load.

**Follow-up (not blocking):** seed the NN test RNG and make the benchmark non-asserting (or tag it),
so a green run is meaningful.

## Notes for follow-up

1. Device smoke per checklist §4 — in particular dashboard ↔ original Overview skin toggle, AIMI
   JSON/CSV writes, physio Health Connect, hormonitor export, Eversense / ONE+ listing, and the
   **fork's low-glucose alarm + "Hypo treated" action** (untouched by the merge but on the critical path).
2. ~~AIMI `invalidInputs` TDD check + abort observability~~ — **done**, see the section above.
   Remaining nit in the same area (deliberately not touched): `OpenAPSAIMIPlugin` lines ~987-989
   declare `val variableSensitivity = 0.0` and `val tdd = 0.0`, both immediately shadowed inside the
   `if (true) { … }` block. They are dead today, but if the `OapsProfileAimi` assembly is ever moved
   out of that block, `variable_sens` and `TDD` would silently become `0.0` and the guard would abort
   every dynISF tick. Worth a separate cleanup commit (drop the dead vals and the `if (true)`).
3. Still open from 2026-08-03: the 15 remaining `:implementation` failures (stub
   `getRunningModeActiveAt`, realign `lastBg`) and the `:ui` `GraphViewModelTest` constructor drift.
4. Still deferred from 2026-07-31: port upstream overview dynamic Y-scaling into the dashboard Vico graphs.
5. Consider migrating AIMI's remaining local `java.text.DecimalFormat` helpers to `NumberFormat`
   in a dedicated commit (declined here to keep the merge diff to safety-relevant changes).

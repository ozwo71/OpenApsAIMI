# Merge log — `dev` → `dev_OAPSAIMI_mergeDEV` (2026-07-10)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target
- **Target:** `dev_OAPSAIMI_mergeDEV` (at `d8b587264e` before merge).
- **Source:** `milos/dev` @ `d389d5e1c2` (Plugin self registration).
- **7 incoming commits** since merge-base `40275ce700` (last documented merge 2026-07-08).
- Working tree clean before merge.

## Scope of incoming commits
| Commit | Summary |
|--------|---------|
| `d389d5e1c2` | **Plugin self registration** — Hilt multibind maps moved from `:app` `PluginsListModule` to per-module `*PluginsListModule`; qualifiers `@AllConfigs` / `@APS` / `@NotNSClient` in `:core:interfaces`; dynamic `:plugins:*` and `:pump:*` wiring in `app/build.gradle.kts`. |
| `d99efae0b4` | Fix scheduling alarm from background — `TimerReminderReceiver` + `AlarmManager.setAlarmClock` in `TimerUtil`. |
| `66ade3ba5d` / `5c14baa7aa` | Crowdin / locs updates. |
| `2df1d3b59e` | Automation test threading fix. |

**No changes to AIMI / SMB / autoISF determine_basal logic** in upstream diff → nothing to port into
`DetermineBasalAIMI2` or OpenAPS engines beyond plugin registration.

## Conflicts (1)

| File | Resolution |
|------|------------|
| `app/.../PluginsListModule.kt` | **Keep upstream** (Multibinds only). Fork plugin bindings moved to self-registration modules (see below). |

## Fork plugin self-registration (post-merge)

Upstream removed central `@Binds` list. Fork-specific plugins re-registered in feature modules:

| Module | Plugin | @IntKey | Notes |
|--------|--------|---------|-------|
| `ApsPluginsListModule` | `OpenAPSAIMIPlugin` | 225 | Between SMB (220) and AutoISF (230) |
| `SourcePluginsListModule` | `EversensePlugin` | 445 | Between Dexcom (440) and Aidex (450) |
| `SourcePluginsListModule` | `OttaiPlugin` | 475 | Fork-only source |
| `SmoothingPluginsListModule` | `AdaptiveSmoothingPlugin` | 615 | Fork adaptive smoothie |
| `MainPluginsListModule` | `OverviewPlugin` | 20 | Legacy overview skin switch |
| `SyncPluginsListModule` | `RemoteControlPlugin` | 315 | Fork-only NS remote control |

## Additional merge fix (build)

Upstream dynamic `:pump:*` wiring now pulls every pump module from `settings.gradle`. Fork WIP
`:pump:apex` (never in pre-merge explicit pump list, does not compile) added to `pumpExclusions` in
`app/build.gradle.kts` alongside `:pump:virtual` and `:pump:combov2:comboctl`.

## Fork preservation verified (post-merge tree)

- **AIMI:** `OpenAPSAIMIPlugin` registered; hormonitor export in `DetermineBasalAIMI2`; prebolus
  carry-forward + 25 min delay; F1 Red Carpet guard intact.
- **Adaptive smoothie:** `AdaptiveSmoothingPlugin` registered; `calibratedOrValue` unchanged.
- **Dashboard skin:** `OverviewPlugin` registered; `dashboardOverview` in `MainScreen.kt` untouched.
- **Eversense:** `EversensePlugin` registered; `:plugins:eversense` in `settings.gradle`; `SourceSensor`
  E3/365 unchanged.
- **ML / physio:** `AndroidManifest` permissions (MANAGE_EXTERNAL_STORAGE, Health Connect) untouched by merge.
- **DB vacuum:** `KeepAliveWorker.databaseCleanup` → `runVacuum = false` unchanged.
- **Hormonitor:** no structural changes in upstream diff.
- **AIMI docs:** no `docs/AIMI*.md` touched; no doc references to central `PluginsListModule` bindings.

## Build / test

- `./gradlew :app:compileFullDebugKotlin :wear:compileFullDebugKotlin` — **PASS** (after `:pump:apex` exclusion).
- `:plugins:smoothing:testFullDebugUnitTest` — `AdaptiveSmoothingPluginTest` **PASS**.
- `:plugins:aps:testFullDebugUnitTest` — `LegacyPrebolusMissedDeliveryTest` **PASS**.

## Pending (user, on-device — NON_REGRESSION §4)

- Smoke: plugin list shows AIMI, adaptive smoothing, Eversense, overview skin switch.
- Dashboard ↔ original overview skin toggle.
- AIMI + adaptive smoothie enabled paths.
- JSON/CSV writes (ML); Health Connect physio permissions.
- Automation timer reminder from background (new `TimerReminderReceiver` path).

# Merge log — `dev` → `dev_OAPSAIMI` (2026-07-05)

Follows [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) and
[MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Source / target
- **Target:** `dev_OAPSAIMI` (at `cf17c8da57`, appVersion `4.0.0.0-dev.AIMI.050726`).
- **Source:** local `dev` @ `2e09122620` (= `milos/dev`, up to date; `dev...milos/dev` = 0/0).
- **41 incoming commits.** Working tree clean before merge.

## Scope of incoming commits (low-risk for the fork)
Wear **bolus wizard correction** feature (±correction buttons, screen-off grace, result card redesign,
`correctionU` through `BolusCommit` wire), **"Plugin enable/disable from search"** (search results can
toggle plugins + Config-Builder-style confirmation dialogs), Wizard alarm fix, dependabot bumps
(compose-bom 2026.06.01, composeUi 1.11.4, androidx.sqlite-bundled 2.7.0, androidxHilt 1.4.0,
hilt-navigation-compose 1.4.0, wear tiles/protolayout, uiautomator 2.4.0), Crowdin, tests.

- **No changes to AIMI / SMB / autoISF / determine_basal** (0 files under `openAPSAIMI`; only Wear
  `HeartRateListenerSmoothingTest` touches a "smoothing" name, and it is a test). → **Nothing to port into AIMI.**
- **No Eversense files** (`settings.gradle`, `SourceSensor`, `PluginsListModule`, DI untouched) →
  Eversense constraint satisfied without re-application.
- **No `AppRepository` / `KeepAliveWorker` / `AndroidManifest` / DB VACUUM path** in the incoming diff
  → DB and physio-manifest gates preserved by construction.

## Conflicts (1)
1. **`ui/.../compose/main/MainScreen.kt`** — the only conflict, in the render region. Resolved by
   **combine** (fork base + dev feature ported), **not** ours-only nor theirs-only:
   - **Kept (fork):** the `dashboardOverview` skin switch (`if (dashboardOverview != null) dashboardOverview(...) else OverviewScreen(..., useRingHeroHome = ...)`) and the embedded-dashboard UI layer (FAB, pump dialog, notification sheet, `isDashboardEmbedded`).
   - **Ported (dev):** "plugin enable/disable from search" — 5 new `MainScreen` params
     (`onSearchPluginToggle`, `onConfirmSearchPluginSwitch`, `onDismissSearchPluginSwitch`,
     `onConfirmSearchHardwarePump`, `onDismissSearchHardwarePump`), `SearchResults(revision, onPluginToggle)`,
     and the two `OkCancelDialog` confirmation blocks (`pluginSwitchConfirmation` / `hardwarePumpConfirmation`).
   - The `MainScreen` signature auto-merged (both param sets present); `SearchViewModel` state fields
     (`revision`, `pluginSwitchConfirmation`, `hardwarePumpConfirmation`) auto-merged; `PluginBase` /
     `OkCancelDialog` imports already present in the fork file.

All other overlapping files auto-merged cleanly (incl. `ComposeMainActivity.kt`, `AppNavGraph.kt`,
`DataHandlerMobile.kt`, `MaintenanceViewModel.kt`, `strings.xml`, `gradle/libs.versions.toml`).

## Fork preservation verified (post-merge tree)
- AIMI: contexts (SlowCarbMeal/HypoRecovery) present; hormonitor export in `DetermineBasalAIMI2`
  (×4 `loop_blackbox_v1` / `AIMI_Decisions`); recent fork work intact (prebolus A+B value-check,
  activity basal cap, onset `-1` filter, LLM retry/models, hypo 60 min).
- Eversense: `EVERSENSE_E3`/`EVERSENSE_365` in `SourceSensor` (×2); `:plugins:eversense` in `settings.gradle`.
- Dashboard skin switch: `dashboardOverview` in `MainScreen.kt` + `ComposeMainActivity.kt`.
- ML storage: `storageHelper` in `MainApp` (×4).
- Adaptive smoothing: `calibratedOrValue` in `ExponentialSmoothingPlugin` (+ `AdaptiveSmoothingPlugin`).
- DB: `KeepAliveWorker.databaseCleanup` → `runVacuum = false`; `AppRepository` runVacuum guard intact.
- Physio: `AndroidManifest` Health Connect entries intact.
- AIMI docs: none of the ~30 `docs/AIMI_*.md` touched by the merge.

## Build / test
- `./gradlew :app:compileFullDebugKotlin` (full flavor, all modules, incl. dependabot bumps) — **BUILD SUCCESSFUL** (exit 0).
- `:plugins:aps:testFullDebugUnitTest` `openAPSAIMI.context.* / activity.* / patient.* / compose.*` — **PASS**.

## Pending (user, on-device — NON_REGRESSION §4)
- Smoke: dashboard ↔ original overview skin switch; **plugin enable/disable from search** (new upstream
  feature) + confirmation dialogs; Wear bolus-wizard correction; AIMI + adaptive smoothie enabled paths;
  JSON/CSV writes; xDrip/Dexcom inbox no stall; low-end stability mode ON/OFF.

# Merge `dev` → `dev_OAPSAIMI` (2026-06-28)

- **Source branch:** `milos/dev` @ `989065e9b4` (`mmol conversion 18.0 → 18.0155`)
- **Target branch:** `dev_OAPSAIMI` @ `cc1393cb54` (meal priority bypass for exercise lockouts + Meal Advisor)
- **Merge-base:** `c642da212b`
- **Upstream delta:** 30 commits (Crowdin translations, QuickWizard/QuickLaunch TT presets phone+wear, AAPSClient fixes — HTTP 410 / remote-error dialogs / unpaired visibility, Medtrum bolus tracking, base basal rate overview popup, dagger 2.60 + play-services-location 21.4.0 bumps, jacoco preview exclusion, mmol constant move).

## Merge conflicts (10 files)

| File | Resolution |
|------|-----------|
| `ui/.../main/MainScreen.kt` | **Combine** — kept fork `dashboardOverview` skin switch + preview-chrome structure; ported upstream `masterOrPairedClient` wiring (`val` + `commandsAllowed = masterOrPairedClient` on `OverviewScreen` + `masterOrPairedClient =` on `MainNavigationBar`). |
| `core/ui/.../navigation/ElementType.kt` | **Theirs + fork line** — adopted upstream `ElementVisibility.MASTER_OR_PAIRED_CLIENT` on treatment dialogs; re-added fork `AFREZZA` entry with same visibility. |
| `core/ui/.../preference/AdaptiveDoublePreference.kt` | **Ours + API migration** — kept fork dynamic precision / range-coercion / `preferenceDisplayTitle`; applied upstream `PreferenceVisibilityContext`→`VisibilityContext`, disabled-alpha styling, `enabled = visibility.enabled`, `@ExcludeFromJacocoGeneratedReport`. |
| `core/ui/.../preference/AdaptiveIntPreference.kt` | Same as AdaptiveDouble. |
| `core/ui/.../preference/AdaptiveUnitDoublePreference.kt` | Same (no `@Preview`, so no jacoco annotation). |
| `core/ui/.../preference/PluginPreferencesScreen.kt` | **Ours + type rename** — fork structure kept; `PreferenceVisibilityContext`→`VisibilityContext` (3 sites). |
| `core/ui/.../res/values/strings.xml` | **Ours** — kept fork-only strings (`extendedbolusdeliveryerror`, `aps_mode_title`, `confirm_remove_preset`, `confirm_cancel_temp_target`, `add_new_preset`, `carb/bolus_equal_zero_no_action`, `wizard_no_insulin_required`=“…required!”); theirs side added nothing new. No duplicate keys. |
| `plugins/main/.../res/values/strings.xml` | **Ours** — kept `disabled_loop`, `calculator_label`. |
| `plugins/main/.../res/values-fr-rFR/strings.xml` | **Ours** — kept FR `disabled_loop`, `calculator_label` + upstream `nav_history_browser`. |
| `ui/.../aboutDialog/AboutDialog.kt` | **Combine** — kept both imports (`AppBrandIcon` fork + `ExcludeFromJacocoGeneratedReport` upstream). |

No `ASYNC IMPACT` conflicts. No opportunistic refactors during resolution.

## Transitive break fixed (post-merge)

| File | Issue | Fix |
|------|-------|-----|
| `ui/.../overview/graphs/GraphViewModel.kt` | Upstream **removed** `GlucoseUnit.MMOLL_TO_MGDL` / `MGDL_TO_MMOLL` (now only in `Constants`, value corrected to `18.01559`). Fork dashboard referenced `GlucoseUnit.MGDL_TO_MMOLL`. | `Constants.MGDL_TO_MMOLL` (already imported). |

Sweep confirmed: no other source reference to the removed `GlucoseUnit` constants; remaining `PreferenceVisibility*` hits are comments only.

## AIMI / upstream parity (SMB / AutoISF / sensitivity)

| Upstream change | AIMI / fork impact | Action |
|-----------------|-------------------|--------|
| `OpenAPSAutoISFPlugin`: `GlucoseUnit.MMOLL_TO_MGDL` → `Constants.MMOLL_TO_MGDL` | Constant relocation only, no algorithm change | **No port** |
| Sensitivity plugins: `R.string.sensitivity_shortname` → `sensitivity_plugin_shortname` (plugins/sensitivity module) | Distinct from `core.ui` `sensitivity_shortname` still used by fork overview/graphs | **No port** — core/ui string intact |
| `mmol conversion 18.0 → 18.01559` | Shared `Constants`; benefits AIMI too | **No port** — value now consistent everywhere |

**`DetermineBasalAIMI2` / `OpenAPSSMBPlugin` not touched by upstream** — no SMB/AutoISF AIMI port required this batch.

## Fork invariants verified intact (static)

| Gate | Status |
|------|--------|
| `:plugins:eversense` in `settings.gradle` | OK |
| `SourceSensor` EVERSENSE / EVERSENSE_E3 / EVERSENSE_365 | OK |
| `KeepAliveWorker`: `cleanupDatabase(..., runVacuum = false)` | OK |
| `AdaptiveSmoothingPlugin`: `calibratedOrValue` | OK |
| `OpenAPSAIMIPlugin` in `PluginsListModule` | OK |
| Dashboard skin switch (`MainScreen` `dashboardOverview`) | OK (kept + upstream pairing-gating ported) |
| AIMI / physio / hormonitor / smoothing / `AndroidManifest` / `MainApp` | **Untouched by upstream diff** |

## Eversense merge constraint

Upstream diff touched **no** Eversense file (no `settings.gradle` module churn, no `SourceSensor` change, no `PluginsListModule` / DI change). Constraint satisfied with no manual re-application. See [MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md).

## Verification completed (local)

- `:app:assembleFullDebug` — **PASS** (BUILD SUCCESSFUL in 40m 48s, 0 Kotlin errors, 1084 tasks).

## Verification pending (device) — per [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) §4

- [ ] Dashboard ↔ classic overview skin switch (+ unpaired-client command gating)
- [ ] AIMI JSON/CSV writes
- [ ] Hormonitor export (`patient_story` in event stream)
- [ ] Health Connect / physio grant flow
- [ ] Adaptive Smoothie with linear calibration
- [ ] Eversense native + notification reader paths
- [ ] AAPSClient: HTTP 410 fix, remote-error dialogs, unpaired visibility
- [ ] QuickWizard / QuickLaunch TT presets (phone + wear)

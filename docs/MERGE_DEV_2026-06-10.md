# Merge `dev` -> `dev_OAPSAIMI_mergeDEV` (2026-06-10)

- Source branch: local `dev` @ `19ad3e373e`
- Target branch: `dev_OAPSAIMI_mergeDEV`
- Starting target HEAD: `3c7b44e2a5`
- Goal: integrate latest `dev` without regression on AIMI, Adaptive Smoothie, dashboard/skin switching, ML storage, physio permissions, Hormonitor study structure, or Eversense-native paths.

## Merge decisions

- `app/src/main/kotlin/app/aaps/MainApp.kt`
  Combined upstream background snackbar fallback with the fork startup DB maintenance path.
  Preserved `maintainDatabaseIfDue()` and `maintainDatabaseAtStartup()` with **no automatic VACUUM** on startup.

- `ui/src/main/kotlin/app/aaps/ui/compose/main/MainScreen.kt`
  Kept fork dashboard embedding (`dashboardOverview`), snackbar host wiring, and overview routing.
  Integrated upstream parameters such as `runningModeRemaining` and `isSimpleMode`.

- `ui/src/main/kotlin/app/aaps/ui/compose/main/MainTopBar.kt`
  Kept user manual button and fork simple-mode settings icon handling.

- `ui/src/main/kotlin/app/aaps/ui/compose/overview/OverviewScreenSplit.kt`
  Combined upstream overview changes with fork `useRingHeroHome`.
  Avoided duplicate sensitivity rendering by keeping the existing chips path only once.

- `ui/src/main/kotlin/app/aaps/ui/compose/overview/OverviewScreenStacked.kt`
  Same strategy as split overview: preserve ring-hero home without duplicating sensitivity.

- `ui/src/main/kotlin/app/aaps/ui/compose/overview/graphs/BgGraphCompose.kt`
  Preserved fork graph/dashboard behavior while keeping the merged upstream graph structure.

- `plugins/aps/src/main/res/values-bg-rBG/strings.xml`
  Preserved fork AIMI/autoISF strings instead of collapsing to upstream-only content.

## Fork preservation checks

- AIMI dashboard routing is still present through `SkinProviderImpl`, `SkinDashboardPreferenceSync`, `GeneralSkin`, `dashboardOverview`, and `useRingHeroHome`.
- User can still return to the original overview skin because the skin preference pipeline remains intact and `prefersDashboardHome` is still skin-driven.
- Adaptive Smoothie still relies on `calibratedOrValue`, preserving the non-regression gate for Linear calibration parity.
- Eversense-native module and merge constraints remain present; no DI/module removal was introduced in this merge work.
- Hormonitor exporter still emits `schema_version = 1.3.0` and preserves additive `patient_story` / `thermal_belief`.

## Post-merge hardening applied

- Added missing Health Connect thermal permissions in `AndroidManifest.xml`:
  `READ_SKIN_TEMPERATURE` and `READ_BASAL_BODY_TEMPERATURE`.
- AIMI model copy on startup now goes through `AimiStorageHelper`, then configures `AimiUamHandler` with the resolved runtime path.
- `DetermineBasalAIMI2` ML CSV paths now follow the resolved AIMI storage directory instead of assuming shared storage only.
- `OpenAPSAIMIPlugin` now loads the SMB refinement model and PKPD logger from the resolved AIMI storage directory.
- `PkPdCsvLogger`, `WCycleLearner`, and `WCycleCsvLogger` now align with `AimiStorageHelper` so they can keep working when `Documents/AAPS` is unavailable and the app falls back to app-scoped storage.

## Verification completed

- `:app:assembleDebug` passed before and after the post-merge hardening changes.
- Targeted AIMI non-regression suite passed:
  `AimiSmbTrainerTest`
  `SmbRefinementFeatureSchemaTest`
  `UamInputSchemaValidatorTest`
  `UamHypothesisStateBuilderTest`
  `PhysioLatentStateBuilderTest`
  `PhysiologicalStressMaskBuilderTest`
  `MealAbsorptionPhaseEngineTest`
  `ReplayQualityExportBuilderTest`
  `RecursiveBeliefPhysioGatingTest`
  `RecursiveBeliefJsonlReplayTest`
  `HyperTrajectoryJsonlReplayTest`

## Verification still pending

- The second targeted Gradle suite (`PkPdIntegrationTest` and `ContinuousStateEstimatorHypoRecoveryTest`) could not be re-run because the sandbox hit a Gradle file-lock/socket execution limit during this session. This is an environment limitation, not a product failure signal.
- Device/runtime smoke remains required for:
  dashboard skin switch dashboard <-> original overview
  AIMI JSON/CSV writes with and without shared-storage access
  Hormonitor export path and fallback behavior
  Health Connect thermal permission grant flow on device

# Merge dev → dev_OAPSAIMI_mergeDEV (2026-05-20)

Upstream: Nightscout `dev` @ `d1e22496f4`.

## Conflict decisions (summary)

| File | Decision |
|------|----------|
| `Versions.kt` | **Ours** — `4.0.0.0-dev.AIMI.*` version string |
| `AndroidManifest.xml` | **Combine** — Health Connect (physio) + WorkManager Hilt provider + FGS dataSync |
| `MainApp.kt` | **Combine** — `Log` (ML assets) + `HiltWorkerFactory` |
| `UiInteractionImpl.kt` | **Combine** — Compose routes (dashboard) + dev alarm/announcement |
| `MainScreen.kt` | **Combine** — `dashboardOverview` embed + scene state + `onIobChipClick` |
| `SourceModule.kt` | **Combine** — Eversense activities; workers → Hilt only |
| `NSClientAddUpdateWorker.kt` | **Combine** — `@HiltWorker` + AIMI `ContextManager` NS inject |
| `WearPlugin.kt` | **Combine** — `collectResilient` + throttle on heavy resend |
| `AppRepository.kt` | **Combine** — Room writer PRAGMA + optional `runVacuum` |
| `plugins/aps/build.gradle.kts` | **Combine** — hilt-compiler + Truth + ONNX |
| `aaps-ci.yml` | **Ours** fork/upstream_tag inputs; tag default `3.4.2.3` |

## Non-regression gates (code review)

| Gate | Status |
|------|--------|
| AIMI plugin / loop | Preserved — no change to `DetermineBasalAIMI2` in merge diff |
| Adaptive Smoothie | Preserved — `calibratedOrValue` usage intact |
| Dashboard skin | Preserved — `dashboardOverview != null` branch in `MainScreen` |
| ML JSON/CSV writes | Preserved — `MainApp` model copy + `Documents/AAPS` paths |
| Physio Health Connect | Preserved — manifest activities |
| Hormonitor structure | Preserved — `AimiHormonitorStudyExporterMTR` untouched |
| Eversense | Preserved — see [MERGE_CONSTRAINT_EVERSENSE.md](MERGE_CONSTRAINT_EVERSENSE.md) |
| DataInbox drain | From dev — Dexcom/xDrip/export workers |

## AIMI vs upstream SMB / AutoISF

- **AutoISF** settings PR is upstream-only; AIMI path remains `DetermineBasalAIMI2` when selected in Config Builder.
- **OpenAPS SMB** workers migrated to `@HiltWorker` on dev; AIMI does not use those worker classes directly.
- **Post-merge fix:** AIMI ML workers (`BasalMlTrainerWorker`, `BasalAdaptiveTrainerWorker`, `T3cNeuralTrainerWorker`) migrated to `@HiltWorker` to match dev `LoggingWorker` constructor (`aapsLogger`, `fabricPrivacy`).

## User verification (required before release)

Follow [NON_REGRESSION_CHECKLIST.md](NON_REGRESSION_CHECKLIST.md) §4 smoke matrix.

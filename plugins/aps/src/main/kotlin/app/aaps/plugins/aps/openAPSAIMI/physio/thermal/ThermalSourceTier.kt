package app.aaps.plugins.aps.openAPSAIMI.physio.thermal

/**
 * How the thermal signal was obtained — measured skin/deviation vs inferred recovery proxy.
 */
enum class ThermalSourceTier {
    MEASURED,
    INFERRED,
}
